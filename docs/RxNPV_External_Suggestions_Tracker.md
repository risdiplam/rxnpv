# RxNPV — External Model Suggestions: Final Synthesis

Four models consulted (A, B, C, D) against the `RxNPV_Feature_Overview.md` context document. This is a full rebuild, not an append — the overlap across all four was extensive enough that a clean, consolidated version is more useful than a fourth appended section. Every technical claim that could be checked against the actual code was checked, not assumed; corrections are called out explicitly where a model's premise turned out to be wrong or only partially right.

**Verdict key:** ✅ Build it · 🟡 Worth doing, lower priority · 🔍 Needs real scoping first · ⚠️ Tension with guardrails · ❌ Don't build · ✔️ Already exists

---

## Cross-model consensus — where independent models converged

This is the most useful signal from consulting four separately: when models that never saw each other's output land on the same idea independently, that's real evidence of a genuine gap, not one model's idiosyncratic take.

- **Partnered-economics valuation mode** (C, D) — both independently identified the same real hole: both existing valuation methods assume the company owns 100% of the asset through commercialization. D's framing is the more precise of the two — a third mode risk-adjusting an upfront payment to signing PoS, each milestone to its own gate's cumulative PoS, and a royalty rate on the existing peak-sales build. Pairs naturally with Model A's licensing-comps-database idea, using the exact architecture the M&A and Peak Sales comps already use.
- **Time-aware / dilution-path financing** (C, D) — same core idea, connecting inputs the engine already computes (R&D-to-launch cost, cash runway) into a funding-gap-driven share-count projection at each future point. D's framing is sharp: this fixes "the single most common retail valuation error" — rNPV on today's share count, ignoring that getting to launch usually costs more dilution than the current cap table.
- **Reverse-solve beyond PoS** (C, D) — both spotted the same natural extension of the already-built, already-verified Implied PoS solver: parameterize it to solve for implied peak sales, implied market share, or implied launch year instead, one at a time. Genuinely low-risk since the reverse-solving machinery already exists and works.
- **Portfolio-level cross-case view** (A, D) — same idea, aggregate dashboard across every case tracked, sliced by fair-value-vs-price, catalyst timing, and risk exposure.
- **Structured red-flag / consistency checks** (A, D, and C's dossier red-flag list) — three-way convergence, though from different angles: A and D both propose portfolio/financing-oriented flags (concentration, financing overhang, insider selling); C's version is clinical-trial-specific (small/unrepresentative population, open-label bias, multiplicity risk). These are complementary dimensions of the same feature, not redundant.
- **Evidence/citation tracking for assumptions** (A, C's ledger, C's dossier) — three converging proposals at increasing depth: A's general evidence log, C's confidence-level-and-snapshot-diff refinement, C's clinical-trial-specific dossier. Same underlying need at three levels of granularity.
- **Versioned snapshots** (A, C) — same idea; C adds attributing a value change to a specific driver category (data vs. commercial assumption vs. PoS vs. financing), which meaningfully sharpens A's original framing.
- **Platform/pipeline correlation for SOTP** (C, D) — both independently flagged that a multi-program SOTP can overstate diversification when programs share a platform, biology, or biomarker premise, and both frame it as an optional overlay on the existing risk waterfall, not a forced assumption.
- **Cash tax / NOL treatment** (C, D) — both suggest it, both independently rank it low priority themselves.
- **Competitor timeline visualization** (B, C) — same idea (Gantt-style layering of own and competitor trial readouts), B naming the chart type, C extending it to include exclusivity events.
- **Avoid market-data/price-feed features** (A's own flagged tension, D's explicit closing guidance) — D independently reaches the same conclusion I'd already flagged about Model A's event-study suggestion, from the opposite direction: real-time price feeds, portfolio accounting, and options analytics don't fit the free/local/methodology-transparent architecture. Worth treating this as settled, not just my own caution.

---

## Corrections — where a model's technical premise didn't hold up

Checked against the actual code or package contents, not taken at face value:

- **SEC XBRL for cash/debt/shares (B):** ✔️ already built — confirmed in a prior round, restated here for completeness. `edgarEngine.js` already pulls structured XBRL tags directly.
- **Gross-to-net / launch-access modeling (C):** the framing implies this is untouched. It's not — pricing already uses an explicit "US annual price per patient (ASP basis)" input, a simplified net-of-rebates proxy, not gross list/WAC. The real gap is narrower than presented: going from one net-ish number to a full WAC → gross-to-net% → ramp-to-steady-state decomposition specifically for the early-launch period, not building net pricing from nothing.
- **Multi-dose PK "superposition" (D):** the framing implies this needs building from scratch. The engine already numerically simulates repeat dosing — `tau` (dosing interval) and `numDoses` are real, working config inputs, and accumulation is already visible in the resulting concentration-time chart. The real gap is narrower: making steady-state Cmax/Cmin and an explicit accumulation-ratio number a *stated* output, not something you'd have to eyeball off the chart yourself.
- **QED / PAINS / SA-score "near-zero cost since RDKit is already wrapped" (D):** checked directly against the actual RDKit.js WASM package's type definitions — genuinely not present. The available API surface covers molecular manipulation, format conversion, fingerprinting, and substructure matching; there's no QED function, no FilterCatalog, no synthetic-accessibility scorer anywhere in it. These are real features of full Python RDKit, but the specific minimal WASM build this app uses doesn't expose them. Implementing them would mean writing the scoring logic in JS using the raw descriptors that *are* available (QED is a published, reproducible formula, so this is realistic) — a genuine, scoped build, not a function call.

---

## Everything else, organized by area

### Valuation engine
- **Full-case Monte Carlo rNPV** (A, D independently) — 🔍 Needs real scoping. Extending the proven Peak Sales Monte Carlo approach to the whole DCF (priors on PoS, timing, pricing, cost structure, discount rate) for a true fair-value distribution instead of three fixed scenario points. Both models that raised it also called it the biggest, last-priority item on their own lists — treat it as the eventual capstone, not the next build.
- **Structured red-flag / consistency checks** — ✅ Build it. Low-risk, purely descriptive, audits data the app already has against itself and against live CT.gov status.
- **Dilution waterfall curve** (B) — ✅ Build it. New visualization of already-verified treasury/if-converted math across a price range, not new methodology.
- **Risk-adjusted expected dilution** (D) — a refinement of the financing-timeline build above, not a separate feature: dilute more heavily in delay/failure states, less in success states, shown as a range. Fold into that build, don't scope separately.

### Partnerships and financing (the two highest-conviction new builds)
- **Partnered-economics valuation mode + licensing comps database** — ✅ Build it. Strongest new idea across all four models given the independent convergence.
- **Time-aware / dilution-path financing** — ✅ Build it. The other standout — genuinely fixes a named, common, real valuation error using inputs the engine already has.

### Reference data and comps
- **Licensing/royalty comps database** — folded into the partnered-economics build above as its data layer.
- **Regulatory precedent library** (C) — 🟡 Worth doing. Search approved drugs by indication/endpoint/pathway rather than by name, complementing the existing FDA Lookup (which only searches by a specific drug). Explicitly an analyst notebook, not an approval-probability engine.
- **Patent/IP timeline ledger** (C) — 🟡 Worth doing. Manual entry only, explicitly not reopening the Orange Book decision — a structured way to document what's currently just a single "years to LOE" number.
- **User-defined benchmark packs** (A) — 🟡 Worth doing, workflow convenience more than new capability.
- **Gross-to-net decomposition** (C) — 🟡 Worth doing as an enhancement to the existing ASP input, not a from-scratch build, per the correction above.
- **"Soft" commercial-trajectory benchmarks** (A) — ❌ Not a feature. The model's own framing calls it "mostly manual notes" — nothing to build.

### Portfolio and process tooling
- **Portfolio / cross-case dashboard** (A, D) — ✅ Build it. Real structural gap — every case currently lives in isolation.
- **Calibration log** (D) — ✅ Build it. Record modeled PoS and market-implied PoS before a catalyst, score both against the actual outcome after. Directly extends the already-verified Implied PoS solver, and it's the kind of long-horizon discipline no institutional product bothers to give you.
- **Versioned snapshots with driver-attributed deltas** (A, C) — 🟡 Worth doing, low-lift, no new dependencies.
- **Launch monitor** (C) — 🟡 Worth doing for anyone tracking a commercial-stage name specifically — quarterly actual-vs-model tracking post-approval. Complementary to the Calibration Log (that's pre-catalyst PoS scoring; this is post-launch commercial-execution scoring), not the same feature.
- **Thesis journal** (D) — 🟡 Worth doing, lighter-weight and more narrative than the structured Evidence Log — a different granularity of the same underlying need, not a duplicate.
- **Endpoint-specific templates** (A) — 🟡 Worth doing, low-lift.

### Clinical, regulatory, and scientific depth
- **Evidence-to-assumption ledger / program evidence dossier** (A, C, C) — ✅ Build it. The most-reinforced idea in this entire review across three separate proposals at increasing depth. Scope the general evidence log first (source, date, confidence, thesis per key input), then let the clinical-trial-specific depth (design, endpoint hierarchy, red-flag checklist) be an optional deeper layer within a program's dossier, not a second system.
- **Europe PMC literature integration** (B) — 🟡 Worth doing as one of several source types feeding the evidence log above, not a standalone tool.
- **Platform/pipeline correlation for SOTP** (C, D) — ✅ Build it. Methodologically real — same-platform programs don't fail independently — and both models scope it correctly as an optional overlay, never a forced assumption.
- **Receptor occupancy bridge for PK/PD** (B) — ✅ Build it. Genuine mechanistic improvement, low-risk addition to the already-validated Emax function.
- **Group-sequential / interim-analysis modeling** (D) — 🟡 Worth doing. Real statistical depth for the Trial Outcome simulator — many pivotal designs have interim looks that change both the probability and timing of a positive readout.
- **Multi-dose steady-state metrics as explicit output** (D) — 🟡 Worth doing, smaller than framed per the correction above — expose what the engine already computes, don't rebuild it.
- **QED / PAINS / SA-score** (D) — 🔍 Needs real scoping, not near-zero cost per the correction above. Worth doing eventually since QED specifically is a reproducible formula over descriptors already extracted, but budget it as a real build.
- **CT.gov snapshot-and-diff** (D) — ✅ Built. New standalone "Trial Watch" tab, not tied to any case — a real program can have several relevant trials, and forcing a one-trial-per-program data model onto that would have been the wrong shape. Each check saves a snapshot (status, phase, enrollment, primary completion date, primary endpoint text) keyed by NCT ID and diffs it against whatever was saved last time; the first check on any trial just sets the baseline, correctly shown as "nothing to compare yet," not an error. Required extending the CT.gov integration's parseStudy with primaryOutcomes (the actual endpoint text) and a new direct single-study lookup (`GET /studies/{nctId}`, confirmed via the official field reference before building on it, not guessed) — the app previously only ever searched by criteria, never fetched one specific trial directly. A watchlist of every previously-checked trial persists across sessions with re-check and forget actions per entry. Hand-verified the diff logic catches all four tracked change types at once (status, enrollment, completion date, and endpoint swaps shown as added/removed rather than a single before/after string, since a list changing isn't the same kind of event as a single value changing), confirms zero changes on genuinely unchanged data, and confirmed error handling for invalid NCT ID formats, a real 404, and a network failure.
- **FAERS disproportionality (ROR/PRR)** (D) — 🟡 Worth doing, needs careful, explicit caveat labeling (not incidence, quarterly lag) given the app's existing discipline around not overstating what a number means.
- **Insider transactions (Form 4)** (D) — ✅ Built. 13F institutional holdings is a genuinely different filing type and stayed out of scope — this covers Form 4 insider buys/sells specifically, the "classic retail signal" half of the original suggestion. Turned out to need real research to get right, not the quick add "EDGAR's already integrated" first suggested: Form 4s are filed under the *insider's* CIK, not the issuer's, so the company's own submissions endpoint doesn't list them — discovery goes through the same full-text search (`efts.sec.gov`, `ciks=` filter) the catalyst calendar already uses, then each matching filing's actual ownership XML gets fetched and parsed for the structured transaction data (shares, price, transaction code) that search-result metadata alone doesn't carry. The XML parser was hand-verified against a real, live SEC Form 4 document fetched directly from sec.gov — every field (issuer, owner, role, shares, price, transaction code) confirmed to match the actual filing exactly, not a synthetic example. The main-process IPC bridge already gracefully handled non-JSON responses without any changes needed there; only a renderer-side function to actually read that fallback text field was missing, for XML instead of JSON.
- **13F institutional holdings widget** — still open, genuinely separate from the above (quarterly institutional positions, not insider transactions — a different filing type, different signal, different discovery mechanism).
- **Advisory-committee dates in Catalyst Calendar** (D) — 🟡 Worth doing, small, admittedly semi-manual since there's no clean API for this specifically.
- **Regulatory-risk overlay tags** (A) — 🔍 Good instinct, tighter scope needed. Keep visible and human-actionable; no automatic linkage to discount rate or scenario weights, not even opt-in — that kind of soft mechanical link tends to drift from optional to unexamined-default over time.
- **Cash tax / NOL schedule** (C, D) — 🟡 Worth doing, both models rank it low themselves — matters once a name approaches sustained profitability, little relevance for typical Phase 1–2 names.

### Flagged tensions with existing guardrails
- **Event-study price-history hooks** (A) — ⚠️ Real tension, now reinforced from the opposite direction by D's explicit caution against market-infrastructure-flavored features. Every existing integration is an official government API; a free price feed is a meaningfully murkier category, closer in risk profile to the already-declined Orange Book and ADMETlab integrations than to anything currently in the app.
- **Position-sizing helper** (A) — ❌ Don't build. Functionally investment-advice territory regardless of "non-prescriptive" framing, cuts against the no-recommendations discipline the whole app is built around.

---

## The committed plan

Not a loose ranking — this is the actual sequencing, with the reasoning for why things are ordered this way, not just what they are.

### Short term — first sprint, in order — ✅ COMPLETE
1. **Evidence Log (general version)** — ✅ Built. Per-program section, freeform-labeled entries (not bound to a specific field name), classification/confidence dropdowns matching the Biotech Agent's exact vocabulary.
2. **Structured red-flag checks** — ✅ Built. Five checks (PoS override, COGS, R&D timeline, peak share, cash runway vs. launch timing), all auditing data already in the app. One real bug found and fixed along the way: an untouched new case was false-flagging on its unset ($0-defaulting) cash field.
3. **Reverse-solve beyond PoS** — ✅ Built. Solves for implied peak revenue, peak market share, or launch timing, reusing the Implied PoS solver's binary-search pattern. Round-trip verified for all three. Surfaced and fixed a real, previously-latent core-engine bug along the way: fractional launch years produced non-integer array indices, silently zeroing out cash flows for years landing on the fraction — fixed at the single source function so it protects every downstream consumer.
4. **Calibration log, started now despite being small** — ✅ Built. Records a PoS prediction (yours and the market's) before a catalyst, an outcome after, and computes Brier scores for both — verified against hand-calculated reference values.
5. **Receptor occupancy bridge** — ✅ Built. Optional K_D input bridges concentration to target engagement before the Emax curve; verified the Hill-Langmuir equation's exact-50%-at-C=K_D property holds through the full UI pipeline.

None of this sprint touched the core valuation engine's methodology — the one exception was the launch-year bug fix, which corrected existing, already-shipped logic rather than adding anything new to it.

### The two big ones — sequenced, not simultaneous
6. **Dilution-path financing** — ✅ Built. Projects when cash would run out (reusing the same cash-flow machinery as the runway calculator) and models a raise there — at current price less an assumed discount, sized to restore a maintained buffer plus cover a set number of forward months — repeated through to launch, plus optional continuous SBC share creep. An alternative to the single future-raise overlay, not additive with it. Only affects the diluted-share divisor, never adds raised cash to net cash (would have double-counted the same R&D spend already reflected as negative cash flow in the NPV — verified this distinction explicitly). Three real bugs found and fixed during verification, not before it: a units error inflating cash balances by ~1,000,000x (traced to `MillionsField`'s actual raw-dollar storage convention, which I'd misread); a scenario-composition bug where forcing "unrisked" R&D cost didn't reliably reach exactly 100% when a Bear/Bull scenario's own PoS multiplier was also in play, silently understating Bear's burn; and a missing-price edge case where a blank current price fell through to a $0.01/share floor and produced billions of shares from a normal-sized raise, fixed by removing the floor so raising is correctly skipped without a real price. Differentiation across Bear/Base/Bull confirmed genuine with an extreme test case (10% vs. 500% revenue), though realistic Bear/Bull multipliers don't always cross a raise threshold differently in every case — that's an honest property of the model, not a flaw.

7. **Partnered-economics mode** — ✅ Built (the comps database piece below is still open). Built as an optional overlay on Quick/Detailed, not a third peer mode — Quick and Detailed both answer "what's total peak revenue," partnership answers "how much of that flows to the company," which is a different axis, not a third way of building the same number. Royalty substitutes the partnered territory's revenue rather than adding to it (verified this doesn't double-count against Detailed mode's own ex-US modeling — Quick mode has no US/ex-US split at all, so only whole-asset partnership applies there). Upfront is a direct undiscounted add; each milestone is risk-adjusted to its own gate's cumulative PoS and discounted from its own expected timing, not lumped in with launch — verified against hand-calculated values to the cent, including the edge case of a milestone tied to an already-passed phase (correctly treated as certain, undiscounted). Cost-sharing reduces the company's own R&D burden by the partnered %. All of this reuses the exact machinery already governing PRV (same risk-adjustment, same discounting, same "self-contained additive contribution to equity value" pattern), so it's one more instance of an established shape rather than new architecture.
   - **Licensing/royalty comps database — ✅ Built.** 8 deals, each multi-source-verified against the companies' own press releases and, wherever available, primary SEC filings (8-K, 10-K, 10-Q, 6-K) rather than a third-party deal tracker alone — several other candidates found during research had inconsistent figures across sources and were left out rather than resolved by guessing. Spans $6.5M to $1.35B upfronts and 5 therapeutic areas, including one very recent (January 2026) mega-deal for current relevance. Royalty ranges are only shown where a specific numeric range was actually disclosed — several real deals only ever say "tiered royalties" or unqualified "double-digit," and those are stored as genuinely unknown (null) rather than filled in with an assumed figure; a 50/50 profit-share deal (AstraZeneca/Daiichi Sankyo) is explicitly labeled as not royalty-based at all, rather than forced into the same royalty fields as the others. Exports directly into a program's Partnership Economics overlay (upfront and royalty midpoint), and correctly leaves an existing royalty rate untouched when exporting a null-royalty deal rather than silently blanking it — verified this specific edge case explicitly. Custom-add/edit/delete included, reusing the exact same generic form component and localStorage pattern already proven for the M&A and Peak Sales comps, per the explicit request to support custom entries here too.
   - Two real bugs surfaced during testing, both traced to my own test scripts' DOM traversal, not the app: one where a broad ancestor-matching selector caused an export test to grab the wrong deal's values (root-caused via an isolated, DOM-free unit test of the export math, which confirmed the calculation itself was exactly correct before touching the UI test again); another where `.textContent` equality matched a wrapper div instead of its leaf label child, since an `<input>` contributes no text of its own. Both fully diagnosed and fixed before treating the feature as verified.

### Medium term — genuinely better for waiting, not just deferred
8. **Portfolio view** — ✅ Built, right on schedule per its own stated reasoning: the prerequisites (evidence-logged assumptions, realistic runway via dilution-path financing) were genuinely in place by the time this got built. A new top-level view aggregating every saved case's already-computed valuation — summary table (price, fair value, upside, runway, modeled and implied PoS), a PoS-dispersion ranking, a cash-runway ranking (shortest first, under 12 months flagged), and a modeled-vs-implied PoS scatter reusing the existing chart component. No new data source anywhere — every number comes from re-running each case's own existing valuation/runway/PoS functions, nothing case-specific gets written back. A case with incomplete or broken inputs is included with a visible error flag rather than silently dropped or crashing the rest of the portfolio — verified this explicitly with a deliberately malformed case. Backward-compatible with cases saved before any feature built this whole session existed, confirmed with a case missing every one of them at once.
9. **Versioned snapshots** — same "start early, compounds over time" logic as the calibration log, just a bit more effort to build.
10. **Platform/pipeline correlation for SOTP**, **regulatory precedent library**, **group-sequential trial modeling**, **13F institutional holdings** — all genuinely good, none urgent. (Insider/Form 4 transactions and CT.gov snapshot-and-diff, previously listed here, are now built — see above.)

### Capstone — unchanged
11. **Full-case Monte Carlo rNPV** — ✅ Built. The capstone, built last exactly as planned. 3,000 trials per run, sampling PoS, peak share, and discount rate continuously from triangular distributions anchored to each case's own Bear/Base/Bull bounds (respecting any per-case override) instead of only three fixed points — reuses `computeCaseValuation` itself unchanged for every trial, plus the same triangular-sampling and Pearson-correlation functions the Peak Sales Monte Carlo in Simulation already uses and has been verified against. No new valuation methodology anywhere in it. One genuine, worthwhile discovery during verification: the simulated median doesn't exactly match the Base-case point estimate, and I didn't wave that away — traced it to a real, expected property (discounting is mathematically non-linear, so uncertainty in the discount rate alone pulls the distribution's center measurably below the naive point estimate), confirmed it precisely by isolating each varying input individually, and it's now stated plainly in the UI rather than left as a confusing discrepancy. Extracted the Bear/Bull override-resolution logic out of the UI layer into a shared function during this build, since the Monte Carlo needed the exact same resolution the existing scenario comparison already had — verified the refactor byte-identical to the original before building anything on top of it.

### Post-capstone polish — self-identified, not from the original four-model suggestion set
After the capstone, did an honest audit of the Simulation section (all 6 tabs, read the actual implementation rather than go by tab names) and of cross-tool connectivity generally, rather than manufacture busywork. Conclusion: Simulation's 6 tabs already cover every real dimension of uncertainty worth modeling — the only genuine gap found was connectivity, not breadth, since these tools had zero export/cross-reference links despite the Tools tab's comps having them throughout. Three concrete items came out of that audit, all ✅ built:
- **Peak Sales Monte Carlo → case export.** The one significant piece of new plumbing, since TrialSim's Peak Sales tab is vanilla-DOM code with no React access, while `cases`/`updateCase` live in React state. Built a standard bridge (`SimulationView` republishes `cases`/`updateCase` onto a plain `window.pdcfSimBridge` object on every render; TrialSim's own export button reads/calls it directly) rather than restructure either side. Exports only the P50/median — deliberately not P10/P90 as separate options, since offering those would suggest a precision a single click doesn't have. Same explicit, one-click pattern as the Tools tab's comps, never automatic. Verified the trickiest part specifically: switching between two cases with different-named programs correctly rebuilds the program dropdown and the export correctly targets whichever case/program is currently selected, not a stale reference to the first one loaded.
- **Company Lookup → Trial Watch link.** A "Watch this trial" link next to each trial in a company's pipeline results, pre-fills and auto-runs the check on arrival rather than making the user re-type the NCT ID they just clicked.
- **Partnership Economics → Licensing Comps link.** Only shown once Partnership is actually enabled (not before), takes the user straight to the Licensing Comps tab to sanity-check the royalty/upfront terms they're entering. Required a genuine cross-view navigation mechanism (App owns the "which tab to land on" request, since Partnership lives in Workspace and Licensing Comps lives in Tools) — built once, at the App level, and reused for both this and the Trial Watch link where the routing was simpler (both siblings within Tools, so no App-level routing needed for that one specifically).


### Cut, not just deprioritized
**User-defined benchmark packs, advisory-committee dates, thesis journal, cash tax/NOL schedule, gross-to-net decomposition, DailyMed** — all real ideas, none worth the calendar space given everything above. **QED/PAINS/SA-score** drops here specifically because it turned out to be a real build, not the near-zero-cost add it was pitched as — the honest cost-benefit doesn't clear the bar it looked like it cleared. **Launch monitor** is cut for now because it only matters for commercial-stage names, a narrower slice than most of what's above.

**Position-sizing helper** and **event-study price hooks** were never "later" — they're not happening, for the guardrail reasons already covered above.

---

## Rename to RxNPV, then a user-driven bug-fix and restructure pass

The above tracker covers the original four-model external-suggestions review and its build-out. What follows is a separate, later engagement: renamed the app, then worked through an itemized list of issues the user found using it for real trials, prioritized lowest-impact-first per their instruction, executing autonomously through each phase per their standing "just continue through your list" direction, with a comprehensive bug check reserved for the end.

### Phase 0 — Rename to RxNPV
✅ Done. Renamed throughout: source, build output (`electron/rxnpv.html`), package identity (`com.rxnpv.app`), window title, export filenames, EDGAR user-agent string, docs filenames (this file included). The one real risk — Electron's `userData` directory is derived from the app name, so a bare rename would silently orphan every existing case, custom comp, and calibration log already saved under the old profile — was closed with a startup migration that only fires when the new profile has no committed data yet and the old one does (checked via presence of a real `.ldb` file, not mere directory existence — Electron itself pre-creates an empty profile before any app code runs, which broke the first, naive version of this guard). Verified end-to-end: the real "Stok Therapeutics" case migrates and loads correctly, and every live integration (EDGAR, CT.gov, RDKit, desktop IPC) still functions post-rename with zero console errors. Caught and fixed a genuinely dangerous side effect along the way: `test/setup.js` still pointed at the old build filename, so a full regression run had silently "passed" against a stale pre-rename fixture instead of failing loudly — fixed and confirmed a genuine pass from a fresh build afterward.

### Phase 1 — Bug fixes from first real-trial usage
✅ Done. Four items, all from direct usage rather than a suggestions review:
- **Delete confirmation.** Case and program deletion previously fired immediately on click with no undo. Added a generic, reusable `ConfirmDialog` (Escape/backdrop-click to cancel), wired to both delete paths. Verified Cancel is a true no-op and Confirm performs exactly one deletion — not just that a dialog visually appears.
- **"Jump to" workspace navigation**, reported as "buggy and hit or miss." Root cause was two compounding issues, not one: an `IntersectionObserver`-based scroll-spy whose rootMargin band could miss short sections or sections at the very end of a scrollable page, and a "click sets active state, then scrolls" pattern racing against that same live observer during the scroll animation. Replaced with direct scroll-position calculation plus an explicit "currently auto-scrolling" flag that suppresses the observer until the scroll genuinely settles. Verified by sampling the highlighted nav state throughout a real scroll animation, not just checking the end state.
- **Light-mode color contrast**, reported as "jarring... some colors too dark while text is too light." Computed WCAG contrast (relative luminance, not eyeballed) for every theme token against all four light-mode background tokens and found 9 real failures — an earlier contrast pass had only checked one background token, missing that the header bar's background is darker than what was checked. Fixed with small hue-preserving darkenings. Fixing that surfaced a second, architecturally distinct bug: darkening a token for its TEXT role broke its existing use as a solid BUTTON FILL in the opposite direction (near-black text on a now-too-dark teal fill). Introduced separate `--teal-fill`/`--red-fill` tokens to decouple the two roles, which also caught a previously unflagged dark-mode failure in the storage-full warning banner that the normal navigation sweep never exercises. Audit went from 9 failures to 0 across both themes.
- **PoS scenario-card label**, reported as confusing "no base case POS modifier" — the user's read was that Bear/Base/Bull showed a probability multiplier rather than an actual probability of trial success, and correctly flagged that PoS "cannot possibly be over 100%." The underlying math was already correct and already properly clamped 0–100% (confirmed by reading the clamp site directly) — this was purely a mislabeled UI value. Fixed to show the program's actual resulting PoS for a single-program case (hand-verified against the known Oncology Phase 2 reference case: 6.1% / 8.7% / 11.3% for Bear/Base/Bull), falling back to the "% of modeled PoS" framing only for multi-program cases where a single number wouldn't mean anything.

### Phase 2 — Restructure: cut Chemistry, consolidate stats tools, relocate live-research tools
✅ Done. Simulation's original 10 tabs read as sprawl rather than depth, and Chemistry specifically was judged (by the user, after being asked directly) not worth keeping — "probably a pointless feature." Also folded in the user's rescinded "one tool per tab" rule ("a lot of these have similar or overlapping functions and would probably work better together... which would also declutter things a bit").
- **Chemistry cut entirely** — not just hidden. Removed the tab, its renderer, and the RDKit WASM loading machinery end-to-end: the script tag in `shell.html`, the `getRdkitWasmUrl` IPC handler in `main.js` and its preload exposure, the `asarUnpack`/`extraResources` packaging config, `src/ts_chemEngine.js`, and the 7MB `RDKit_minimal.wasm` binary itself. Confirmed zero leftover references inside the packaged `.app`'s `app.asar` afterward.
- **Fragility Index, Sample Size/Power, P-value↔CI, and Single-Arm CI consolidated** into one "Trial Statistics" tab with its own sub-navigation, replacing four top-level tabs that all did the same kind of narrow, already-in-hand-result statistical work.
- **FDA Lookup relocated** from Simulation into Tools' Live Research group, alongside Company Lookup, Catalyst Calendar, and the tool below — this is live external-API research, the same category of work as those, not a simulation. Ported from vanilla-DOM to a React component; functionally unchanged (full detail deepening is separate, later work).
- **Historical Comps merged into Trial Watch**, forming a new "Trial Explorer" tab — a condition/phase comparable-trial search stacked above the existing per-NCT-ID snapshot-and-diff watchlist, since both are the same underlying question (what does the trial landscape around my thesis look like) at different granularity.
- **A real, previously-shipped bug found and fixed along the way, unrelated to the restructure itself but surfaced by touching this code**: `fdaEngine.js` and `ts_fdaEngine.js` both declared a global `fdaFetch` function with incompatible signatures (one takes a full URL, the other a path+params pair) — no module system means the later-loaded one silently overwrote the earlier one at the global scope. This meant `searchDrugApproval` (used by Peak Sales Comps' "Check real FDA approval" button) had been broken since `ts_fdaEngine.js` was merged in — every call produced a malformed double-domain URL and silently failed with a caught error message, never a crash, so it was never visibly broken. Fixed by renaming the TrialSim-side function to `tsFdaFetch`. Verified live against the real openFDA API post-fix: Keytruda now correctly resolves to BLA125514, approved 2014-09-04, Merck Sharp Dohme. Also fixed a small cosmetic redundancy this same live check surfaced for the first time (the button had never worked before, so this line had never actually rendered): the result line showed "BLA BLA125514" because `applicationNumber` already carries its type as a letter prefix and `applicationType` just re-extracts that same prefix.
- All test-tab-list assertions in `test/final_sweep.js` and the standalone-tab navigation in `test/new_features_test.js` updated to match the new structure — both were exercising the old 10-tab shape and needed the same kind of fixture update Phase 0 needed for `test/setup.js`.

### Phase 3 — Deepen the relocated/restructured tools
✅ Done. Four parts, sequenced deliberately (guidance last, against the final structure):
- **Trial Explorer's individual matched trials.** `fetchHistoricalComps` had always returned the full per-trial list (NCT ID, title, status, phase, sponsor, enrollment) alongside its aggregate status-landscape rollup — the old tab only ever rendered the rollup and silently discarded the list. Rendered it, with a direct ClinicalTrials.gov link per trial and a one-click "Watch this trial" into the snapshot-diff tool in the same tab. Verified live: a real NSCLC/Phase 2 search returns 100 real, individually-linked trials.
- **FDA Lookup, deepened.** Previously "half-baked and limited" per the user's own words. Now shows every Drugs@FDA submission per application (was capped at 5) with formatted dates, the two label sections that were already being fetched but never rendered (warnings/precautions, adverse reactions from the label), a much higher truncation ceiling on label text (500 → 3000 chars), and a wider FAERS adverse-event window (10 → 25 reactions). Verified live against warfarin: 51 real submissions across multiple ANDAs, full label sections, 25 FAERS reactions.
- **Export path for the four Trial Statistics tools.** Every chart-bearing Simulation tool already had PNG/SVG/panel export; the four numeric-only tools (Fragility Index, Sample Size/Power, P-value↔CI, Single-Arm CI) had none — a result appeared on screen with no way to get it out of the app. Added a lighter sibling of the existing chart-export helper (panel capture only, since there's no chart to vectorize) and wired it into all four.
- **Guidance, written last.** A new Reference Sheet guide-tab card explains Simulation as a whole and each of its 4 top-level tools, framed via the backward/forward-looking mental model the user endorsed mid-planning (Workspace assumes a PoS/peak revenue/timeline already exist; Simulation is where those assumptions come from — stress-testing data already in hand, or projecting an upcoming trial). Small "Forward-looking"/"Backward-looking" badges added next to each Simulation tab's own header as a persistent inline cue. Also fixed the guide tab's Tools list, found stale independent of this restructure — Licensing Comps had never been listed at all.

### Phase 4 — Data & engine: sourced defaults, a new PoS lever, auto-captured evidence, deeper comps
✅ Done. Four parts:
- **Simple Multiple's default, sourced.** The flat 3x default carried its own honest admission ("not sourced from the app's benchmark data") — the user flagged this directly. Cross-referenced the app's own M&A and Peak Sales comps datasets against each other (no separate research pass) to find 7 real, genuinely single-asset-driven deals appearing in both, hand-verified each deal-value/peak-sales multiple independently. Real multiples span 3.55x-16.22x, median 7.8x — kept the input default at 3 (still user-editable) rather than silently jumping every future case to a 7-comp median skewed by a couple of outliers, and surfaced the full sourced range in the field's tooltip plus a collapsed-by-default precedent-by-precedent breakdown.
- **Case-level Base-PoS control.** A genuinely new lever — the per-program PoS override and the Bear/Bull scenario multipliers both existed already, but nothing could shift Base's own PoS treatment at the case level (Base was hardcoded as the fixed 100% reference point everywhere). Now composes multiplicatively with both existing levers, applied to Bear/Base/Bull alike so their 70%/100%/130% relationship holds relative to the case's own adjusted Base. Required sweeping every direct reference to the raw scenario preset across the app (SOTP live and in the PDF report, both risk waterfalls, Sensitivity's driver sweeps, Portfolio's summary) so the new control is honored everywhere Base actually gets computed, not just the headline card — which surfaced a real, pre-existing bug along the way: the PDF report's scenario cards read the raw preset directly, silently ignoring any per-case Bear/Bull override that already existed. 14 new checks in `test/math_verification.js`, hand-derived independently, plus a live check confirming a reference case's 6.1%/8.7%/11.3% Bear/Base/Bull PoS became exactly 3.7%/5.2%/6.8% at a 60% adjustment.
- **Evidence Log auto-capture.** Every EDGAR-backed capital-structure export (Company Lookup, Diluted Market Cap, Cash Runway, Capital Structure's own EDGAR pull) now appends an Evidence Log entry citing the real source filing at the moment the data is actually applied — previously a sourced fact only ever existed as a transient result panel. Deduped by (label, source, date) so repeated clicks don't spam entries; verified live with a real EDGAR pull for Moderna correctly logging a "fact" entry citing the real filing URL, and a re-export leaving the count unchanged.
- **Comps expansion, continued directly** (confirmed with the user as the established, preferred process — no change in method). Licensing Comps (the thinnest database, 11 deals) grew by 4 real, multi-source-verified deals, three from the PD-1/VEGF bispecific class's unusually dense 2024-2025 cluster plus one non-oncology deal for area diversity. Peak Sales Comps gained Enhertu, a notable real gap. M&A Comps (65 deals) checked for gaps and duplication and found already in good shape.

### Phase 5 — Phase 2 → 3 Translator, a real sourced effect-size shrinkage tool
✅ Done. Externally researched before building, per the plan (the user's own framing: "Simulation will need the most work... may require digging outside of what we currently have"). Early-phase trial results systematically overstate what a confirmatory Phase 3 trial goes on to observe — real, published regression-to-the-mean, not an arbitrary haircut. Two independently-sourced correction factors, one per endpoint type: binary/response-rate (a 2026 concordance analysis matching Phase 3 solid-tumor oncology trials 2014-2020 to their supporting Phase 1b/2 trials found early-phase ORR overstated the eventual Phase 3 rate by "about one-fifth" on average) and time-to-event/hazard-ratio (a JNCI analysis of 111 Phase 3 oncology trials from 2023 found the design-time assumed HR averaged 0.66 vs. 0.72 actually observed — hand-verified the implemented constant reproduces the paper's own two numbers, not just internal self-consistency, before shipping it). New "Phase 2→3 Translator" tab — genuinely both backward (reads an observed result) and forward (projects an assumption) at once, so it earned its own tab rather than folding into either purely-backward Trial Statistics or purely-forward Trial Outcome/PoS. Explicitly flags when a weak Phase 2 hazard ratio shrinks past 1.0 rather than silently clamping it, since that crossing is itself the honest finding — a marginal result that may not survive Phase 3 as a real effect at all. A collapsed note cites the FDA's real "22 Case Studies Where Phase 2 and Phase 3 Trials Had Divergent Results" (2017) for the qualitative causes a single number can't capture, framing the tool as a planning anchor rather than a guarantee.

### Phase 6 — Warrant-data extraction, verified against real filings
✅ Done, as the timeboxed spike it was planned as. The extraction machinery itself (`extractWarrants` in edgarEngine.js, using the real `ClassOfWarrantOrRightOutstanding`/`ClassOfWarrantOrRightExercisePriceOfWarrantsOrRights` XBRL tags) turned out to already exist from earlier work — this phase was the "actually verify it against real filings" step that hadn't happened yet. Live-tested against 6 real biotechs: warrant COUNT extraction genuinely works (4/6 companies returned real counts — Windtree 8.5M, Assembly Bio 10.9M, Enveric 1.4M, Processa 304K), but warrant STRIKE PRICE extraction failed in every single successful case (0/4) — confirming the plan's own prediction that this XBRL tag is essentially never populated in practice, with the real number living in unstructured equity-note tables instead. The underlying code already handled this safely (never guessing a strike, always leaving it for manual entry), but that caveat wasn't visible everywhere a user would hit it — Company Lookup's EDGAR panel didn't mention options/warrants at all despite silently exporting them, and Diluted Market Cap's pull gave no indication a strike was missing until a blank field was noticed later. Both now show the same honest "strike not tagged — check the filing" wording Capital Structure's own panel already used.

---

## Post-plan additions — two new ideas raised after the six-phase plan closed

Same process: research/verify first, hand-verify the math independently, live-test in the packaged app before calling it done. Both delivered together in one pass, per the user's explicit go-ahead.

### Cell therapy + gene therapy modality
✅ Done. Confirmed via FDA's own regulatory framework, not assumed: cell and gene therapy both file as a standard §351 BLA (same legal form as "biologic"), but FDA gave them a genuinely distinct regulatory home in 2023 (CBER's Office of Therapeutic Products, formerly OTAT) with materially different review requirements — real grounds for their own benchmark data. Checked and ruled out other candidates the same way: vaccines are plain BLA with no distinct review treatment; biosimilars (351(k)) are a genuinely separate FDA pathway but for follow-on manufacturers, not the novel clinical-stage assets this app models — noted to the user, not silently dropped.

Sourced real COGS: cell therapy ~22% (cross-checked two ways — a 2019 per-dose CAR-T cost estimate against Yescarta's launch price, and Gilead's own public ~20% COGS target for its CAR-T business), gene therapy ~55% (a genuinely wide 30-90% real range given AAV manufacturing cost of $1-2M/dose against $2.1-3.5M list prices for Zolgensma/Hemgenix — reported as a range, not false precision).

The harder call was exclusivity/erosion: rather than force cell/gene therapy into the existing biosimilar-competition-curve shape, both default to near-zero erosion with an explicit note, since no cell or gene therapy has ever actually lost exclusivity to a follow-on competitor — there's no real precedent to source a curve from. The real revenue-decline mechanism for a one-time-dose product is treatable-patient-pool exhaustion, already captured by the launch curve, not competitive erosion.

Every 2-way modality ternary in the codebase (COGS lookup in 4 places including the DCF's own cost resolution, exclusivity bench text, a red-flag threshold that would have falsely fired on gene therapy's own 55% default) refactored into shared `getCogsBenchmark()`/`getErosionDefaults()` lookups off one `MODALITY_OPTIONS` list.

### Minimum Detectable Effect calculator
✅ Done. The direction Sample Size/Power didn't cover: "given a FIXED N (already enrolling or fully enrolled), what's the smallest effect that would actually read out significant?" Added as a "Solve for" toggle on the existing tool. Same binary-search-over-the-verified-power-function pattern as the existing sample-size solvers, guaranteed consistent with them by construction — verified directly by round-tripping each endpoint type through both solvers and confirming they agree with each other to high precision.

26 new hand-verified checks across both additions (math_verification.js now at 205 total), including a round-trip cross-validation and a "not achievable" edge case that initially exposed a flawed test of my own (an unbounded search ceiling that made "unreachable" impossible to construct) — fixed the test, not the code. Live-verified in the packaged app end-to-end, including a full gene-therapy program run through DCF, Monte Carlo, and PDF report generation with zero errors.

### Comps expansion, continued
Filled a real gap found while checking: 2021 had only 1 M&A deal versus 2024-2025's dense coverage. Added 3 real, verified deals from that year's actual biggest transactions (Merck/Acceleron $11.5B, Jazz/GW Pharmaceuticals $7.2B, Merck/Pandion $1.85B) — 2021 was itself a comparatively quiet M&A year industry-wide per multiple trade-press recaps, so this reflects real relative thinness, not under-research of that year specifically. This remains open-ended by nature, same as noted after Phase 4.

---

## Phases 7-11 — Biostats expansion: 2×2 outcome analysis, non-inferiority, multiplicity, meta-analysis, placebo benchmarks

✅ Done. The user's own framing set the bar for this round: "generating edge, with good visuals to back it all up, not necessarily building an institutional grade tool... there should be something for beginners and something for more advanced investors as well." Explicitly scoped down from 6 raw ideas to 4 consolidated builds plus one reference-data addition, per the user's own stated concern about scope creep — NNT/NNH and RR/OR/RD share one 2×2 input, so they became one tool instead of two; non-inferiority and multiplicity joined the existing Trial Statistics tab as two more backward-looking sub-tools rather than new top-level tabs; placebo response became reference data (a new Reference Sheet card, like the existing comps tables) rather than a calculator. Group-sequential/interim-analysis modeling, raised as a possible idea during scoping, was explicitly NOT reopened — it remains a "deliberately not built" scope decision from the original build, flagged to the user rather than silently built or silently dropped.

**Two new chart primitives** (`ts_chart.js`), both dependency-free hand-rolled SVG, theme-aware via `var(--...)` tokens like the existing histogram/line-chart: `renderIconArray` (a 10×10 dot grid, fraction highlighted — the beginner-facing NNT visual: "for every 100 treated, this many more benefit") and `renderForestPlot` (one flexible primitive covering three uses: a single CI whisker against a null-reference line, a CI whisker against a shaded non-inferiority margin zone, and a multi-study list with a pooled diamond glyph for meta-analysis).

**2×2 Outcome Analysis** (new Trial Statistics sub-tab): risk ratio, odds ratio, risk difference, and NNT/NNH from one 2×2 table, each with a real Wald confidence interval. Deliberately built `computeNNT` to return a neutral `eventsHigherInArmA` fact rather than guessing harm-vs-benefit from the sign of the risk difference — an early version auto-labeled by sign, which is only correct if "events" always means a bad outcome; the UI combines the neutral fact with an explicit user-stated "higher rate means better or worse" selector instead. Live-verified against a real published trial: ISIS-2's aspirin vs. placebo vascular-mortality data (804/8587 vs. 1016/8600) reproduces the trial's own widely-cited RR (~0.79) and NNT (~40-42) almost exactly.

**Non-Inferiority** (new sub-tab): takes an already-reported point estimate + CI and a pre-specified margin, checks whether the entire CI clears the margin (direction-aware). **A real bug found during live verification, not just in source**: `renderForestPlot`'s axis-domain calculation only considered the CI and reference line, never the margin zone bounds — when the margin sits outside the observed CI (the normal case, since that's the whole point of the check), the shaded margin region rendered off-canvas and was invisible, silently defeating the tool's own visual centerpiece. Fixed by folding `marginZone.from`/`.to` into the domain calculation; re-verified live that the shaded region now renders correctly on both sides of the axis.

**Multiplicity Adjustment** (new sub-tab): flat Bonferroni and Holm step-down side by side for up to 8 endpoints, so "testing more endpoints raises the real bar" is visible in a comparison table, not just asserted. **A second real bug found during live verification**: the headline span was labeled "(Holm)" but displayed the Bonferroni survivor count (`survivedBonf`), so a hand-verified example correctly showing the Holm column passing 3/4 endpoints was reported in the headline as only 1/4 — a copy-paste variable mismatch, not a math error (the underlying `holmBonferroniAdjust`/`bonferroniAdjust` functions were and remain correct, confirmed by `math_verification.js`'s existing 9-check section for this exact worked example). Fixed by reading `holm.filter(h => h.significant).length` for the headline; re-verified live that headline and table now agree.

**Meta-Analysis** (new top-level Simulation tab, not a Trial Statistics sub-tab — genuinely both backward AND forward like Phase 2→3 Translator, since it takes multiple already-observed studies and produces a pooled estimate for use going forward): fixed-effect (inverse-variance) and DerSimonian-Laird random-effects pooling side by side, Cochran's Q/I² heterogeneity, forest plot with a pooled diamond glyph. A dynamic add/remove study list (2-8 studies, 3 input modes: 2×2 tables, ratio-scale point estimate+CI, linear-scale point estimate+CI) — the first genuinely dynamic (not just tabbed) vanilla-DOM list built this session, module-level mutable arrays triggering full re-render on add/remove. The most methodologically involved math added this session, hand-verified against two independently-constructed worked examples (one showing real between-study heterogeneity meaningfully widening the random-effects CI, one showing the τ²-floors-at-zero edge case correctly collapsing random-effects to fixed-effect exactly) before wiring into any UI. Live-verified with a real 3-study synthetic dataset: pooled RR landed between the three individual study estimates as expected, forest plot rendered correctly with the amber pooled diamond positioned at the pooled estimate.

**Placebo response benchmarks** (new Reference Sheet card, Probability of Success tab — reference data, not a calculator, per the consolidation decision above): 6 real, sourced entries across the well-documented high-placebo-response areas (depression/MDD ~37.5% responder rate per 30-year antidepressant-trial meta-analytic reviews; chronic/neuropathic pain ~38% at the ≥30% pain-reduction threshold, highly syndrome-dependent (postherpetic neuralgia ~11.5%, HIV-related pain ~43%) per a 2022 50-trial meta-analysis; IBS ~37.5% pooled, endpoint-dependent (composite FDA endpoint runs much lower, ~18-20%) per a 2021 meta-analysis; migraine prevention ~21% (95% CI 13-28%) per a 32-RCT meta-analysis, with newer anti-CGRP antibody trials running meaningfully higher (~29-33%); psoriasis biologics ~4% PASI-75 per a 2012 31-trial meta-analysis — one of the lowest in the set, since psoriasis lesions are objectively scored; atopic dermatitis biologics ~13% EASI-75, markedly higher than psoriasis despite the same organ system, since AD lesions are harder to score objectively). Each entry cites its own specific published meta-analysis rather than an average across sources. Also added a static, non-automatic contextual note in Trial Outcome/PoS pointing to this card before setting a prior — kept explicit rather than auto-wired to a case's therapeutic area, consistent with this app's existing "cross-feature connections are explicit and one-click, never automatic" convention (Partnership Economics → Licensing Comps, Company Lookup → Trial Watch).

**A third, unrelated but adjacent bug fixed while touching this part of the Reference Sheet**: the Probability of Success tab's closing note claimed "these PoS tables aren't wired into calculations yet — that's the next build phase," which was stale — `POS_MODIFIERS` (biomarker use / disease type) has been live in `posEngine.js`'s actual PoS calculation since the session-7 "science→valuation bridge" work. Corrected to describe what's actually wired (the modifiers) versus what's still reference-only (base-case/molecule-type/regulatory tables, not auto-selected by the engine).

41 new hand-verified checks added to `math_verification.js` (now 246 total, 0 failures): 2×2 outcome measures (RR/OR/RD/NNT, using a hand-worked 20/100 vs. 40/100 example with every CI bound independently recomputed, not copied from the implementation), multiplicity adjustment (a p-value set specifically chosen to show Holm rejecting 3 hypotheses vs. Bonferroni's 1), and meta-analysis (two examples: one showing real heterogeneity, one showing the τ²-floor edge case). `test/final_sweep.js` updated for the new tab/sub-tab counts (6 Simulation tabs, 7 Trial Statistics sub-tabs) and confirmed 0 console errors live. Full test suite (10 files) re-run clean after every fix. Packaged app rebuilt three times across this round as bugs were found and fixed, each rebuild followed by a fresh live CDP click-through — not just a source-level re-check — before calling the round done.

---

## Phase 12 — Simulation section audit: independence, math verification, visuals, polish

✅ Done. The user asked to slow down and work one section at a time, starting with Simulation, against four explicit questions: are there real feature gaps, is the math backbone fully accurate and sourced, is everything that could be a chart actually one, and is every tool fully fleshed out. This phase is that audit plus every fix it surfaced.

**Independence check** (do consolidated features still work standalone where there's real value in that): every existing consolidation in Simulation — Trial Statistics' 7 sub-tabs, Sample Size/Power's solve-for toggle, 2×2 Outcome Analysis's four measures from one input — was already one click away with no forced re-entry of data, so nothing needed changing there. Found one genuine gap: `receptorOccupancy()` (a trivial three-number Hill-Langmuir formula) was only reachable by running PK/PD's full concentration-time simulation. Added a standalone Receptor Occupancy Calculator to the PK/PD tab — same already-verified function, a real independent entry point, live-verified against two hand-calculated cases (conc=K_D → exactly 50%, conc=10/K_D=5 → 66.7%).

**Math/data accuracy audit**: every individual stats primitive was already hand-verified, but the orchestration layers wiring them into the two actual Monte Carlo engines — `runAssuranceSimulation` (Trial Outcome/PoS) and `runPeakSalesSimulation` (Peak Sales) — had zero end-to-end verification anywhere, despite a code comment explicitly claiming that cross-check existed. Closed with 31 new checks: exact deterministic checks (point priors, all-fixed Monte Carlo inputs, zero-variance driver correlations) plus convergence checks against the already-verified closed-form power formulas for all three endpoint types. No bugs found — confirms the orchestration was already correct, just unproven. Also fixed a real test-harness-only bug surfaced while writing these checks: `new Function(...)`-based test evaluation doesn't replicate a browser's "top-level function declarations attach to `window`" behavior, so the `STATS =  this` indirection pattern in `ts_simulationEngine.js`/`ts_peakSalesEngine.js` silently resolved to an object missing every function it needed. Added a small harness-only shim; no app behavior changed. `math_verification.js` now at 277 checks, stable across repeated runs (unseeded randomness).

**Visual audit and fixes** — the clearest gap found: five tools showed only a bare headline number with zero chart (Phase 2→3 Translator, Fragility Index, Sample Size/Power, P-value↔CI, Single-Arm CI), and Peak Sales' driver sensitivity was a plain bulleted list of correlation numbers instead of a chart. Two new chart primitives added to `ts_chart.js` — `renderTornadoChart` (horizontal bars from a zero baseline, color-coded by direction, for ranking driver influence) and a `markerX`/`markerLabel` option on `renderLineChart` (a vertical reference line, matching the marker already on `renderHistogram`) — plus per-row `color` support added to `renderForestPlot` for cases needing more than one semantic color per chart (raw vs. Bonferroni vs. Holm in Multiplicity Adjustment). Wired in:
- **Peak Sales**: driver list replaced with a tornado chart.
- **Phase 2→3 Translator**: a two-point comparison (observed Phase 2 vs. shrinkage-adjusted Phase 3) on a zero-width-whisker forest plot, for both endpoint types.
- **Fragility Index**: an icon array showing the flip path as a share of that arm's patients.
- **Sample Size/Power**: a power curve (power vs. N, or power vs. effect size in Minimum Detectable Effect mode) for all three endpoint types in both solve directions — 7 combinations total, all reusing the already-verified closed-form power functions rather than any new math.
- **P-value↔CI and Single-Arm CI**: a single CI whisker on a forest plot.
- **Multiplicity Adjustment**: a 3-dot-per-endpoint plot (raw/Bonferroni/Holm) against the alpha threshold, color-coded pass/fail — verified live to reproduce the exact hand-worked `math_verification.js` example (Key secondary and Secondary 2 correctly flip from red/fail under Bonferroni to teal/pass under Holm).
- **Receptor Occupancy Calculator**: an occupancy-vs-concentration curve with the current result marked, rounding out the tool added earlier in this same phase.

**A second polish gap found while doing the visual pass, not originally planned**: 2×2 Outcome Analysis, Non-Inferiority, and Meta-Analysis already had real forest-plot charts from the prior biostats-expansion phase, but were still using the lighter numeric-only "Capture result" export instead of the full PNG/SVG/Panel export every other chart-bearing tool gets — charts existed but couldn't be exported as charts. Fixed by switching all three (and 2×2's three separate charts individually) to the full chart-export helper.

Every new/changed tool live-verified in the packaged app via CDP after rebuild and packaging — all 6 Simulation tabs and 7 Trial Statistics sub-tabs, all 7 Sample Size/Power endpoint-and-mode combinations, all P-value↔CI direction/scale combinations — zero console errors, zero exceptions. Full 10-file test suite re-run clean throughout.

---

## Phase 13 — Simulation beta-readiness: chart quality, interactivity, survival curves, edge-case bug sweep

✅ Done. The user asked, before moving to Tools, whether Simulation had any remaining feature gaps, whether a full bug check had been done, and whether the charts were "attractive, intuitive, and interactive where necessary" — with the section to be fully beta-ready first.

**Chart quality — three real defects found by inspection, not assumed:**
- **No Y-axis tick labels anywhere.** Neither `renderLineChart` nor `renderHistogram` emitted a single Y-axis number. The receptor-occupancy chart said "% occupied" with no scale; the power curves said "Power" with no scale. Every value on them had to be estimated. Added `niceTicks()` (1/2/5 x 10^k step sizes) plus Y-axis labels, X-axis labels, and subtle gridlines to both.
- **No interactivity at all.** Every chart was fully static. Added native SVG `<title>` hover tooltips to the histogram (bin range + count + % of total), line chart (a capped ~60 sampled points per series, each showing its exact x → y), forest plot (a full-row hover strip giving estimate + CI), and tornado chart (exact correlation + direction). Chosen deliberately over JS event wiring: zero dependencies, no listeners to leak on re-render, and it survives PNG/SVG export intact.
- **Axis precision loss.** The forest plot formatted ticks with a flat 2 decimals, so a p-value axis could render two genuinely different ticks as the same label. Added `formatTick()`, which derives decimal places from the actual step size.

**Two real bugs found and fixed during this phase, both in code written earlier in this same session:**
- **`formatTick` trailing-zero bug, caught by its own new test.** The strip regex `/\.?0+$/` made the decimal point optional, so it also ate the zero in whole numbers — an axis tick of `20` rendered as `"2"`, `100` as `"1"`. Caught immediately because the tick logic was given hand-worked tests rather than being treated as "just presentation code"; fixed to strip trailing zeros only after an actual decimal point.
- **A UI honesty bug in the PK/PD guidance note.** The note added last phase claimed F (bioavailability) "is disabled for that route" on IV bolus. Nothing disabled it — there was no route-change handler at all, and Ka/F stayed fully editable while being silently ignored by `concIVBolus()`. Rather than soften the note to match the weaker reality, implemented what it promised: `syncPkpdRouteFields()` now greys out and disables Ka and F on IV, with an explanatory tooltip. Same class of gap as the Phase 6 warrant-strike honesty fix.

**Axis-resolution refinement:** the initial `niceTicks` used exact-value thresholds (`norm <= 2 ? 2 : ... 5`), which sent a raw step of 0.209 straight to 0.5 — labelling a 0..1 power axis as only 0 / 0.5 / 1, too coarse to see where the 80% power target falls, which is the one thing a power curve exists to show. Switched to the standard geometric-midpoint thresholds; the axis now reads 0 / 0.2 / 0.4 / 0.6 / 0.8 / 1 and the curve visibly crosses 0.8 exactly at the solved-n marker. Pinned with its own regression check.

**Feature gap closed: survival curves.** Trial Outcome/PoS's time-to-event mode computed hazard ratios and event counts but never drew the two survival curves the hazard ratio implies — the single most standard visual in oncology trial reporting, and the one genuine missing-visual gap flagged in the prior phase's audit. Added, using the same exponential (constant-hazard) survival model the replicate simulator itself uses, so it pictures the assumption being simulated rather than introducing a second model. States the resulting median-survival gain in plain language and is explicit that real curves with a time-varying hazard differ in shape. Hand-verified: median control 12 months at HR 0.7 gives median treatment 12/0.7 = 17.14 months, matching the app exactly.

**Edge-case bug sweep** across the live packaged app, zero console errors and zero uncaught exceptions throughout: Fragility Index at a non-significant input (correctly skips the icon array) and at the 0-vs-50 extreme; 2x2 Outcome Analysis on its crosses-null path (correctly suppresses NNT and its icon array while still drawing both forest plots); PK/PD route switching in both directions plus the K_D-set receptor-occupancy branch (3 charts) and an IV bolus whose Cmax hand-checks exactly to dose/Vd = 500/50 = 10 mg/L; Meta-Analysis data-entry mode switching with studies already added, plus add/remove and the correct disappearance of Remove buttons at the 2-study floor; Non-Inferiority in the "above" direction on both a passing and failing case; Single-Arm CI at both the 0/N and N/N boundaries; Sample Size/Power on an unreachable effect size; Peak Sales with a Normal input wide enough to sample negative (correctly clamped, no NaN). Dark mode verified for every new chart element — gridlines, tick labels, and tooltips all theme-aware via CSS vars.

`math_verification.js` now at 295 checks (0 failures), including the new axis-tick section. Full 10-file suite clean.

**Beta-readiness double-check (same phase).** Asked to confirm Simulation was genuinely beta-ready before moving to Tools, a re-audit found one real gap that every prior check had missed because it lives outside the Simulation code: **the Reference Sheet's "How This Works" guide — the app's own in-product documentation — was never updated for any of this round's work.** It still described Trial Statistics as "Four backward-looking tools" (it has seven) and made no mention at all of 2x2 Outcome Analysis, Non-Inferiority, Multiplicity Adjustment, the entire Meta-Analysis top-level tab, the standalone Receptor Occupancy Calculator, or the new survival curves. A user reading the app's own guide would not have known four of the section's tools existed. Rewritten to cover all of them.

Also verified in this pass, none of which had been checked before: **every one of the 13 Simulation tools driven end-to-end in the packaged app** (each produces a real result, 0 console errors and 0 console warnings), with spot values cross-checked against the suite's own hand-verified references (NNT = 5, n = 163/163, CI 0.477-9.523, occupancy 66.7%); and **the chart export path**, which had never been exercised at all — confirmed that the new `var(--rule)` gridlines bake correctly to a literal hex on export (no unresolved `var(` survives), that the 40 hover-tooltip `<title>` elements survive serialization, and that PNG rasterization produces a real image. That last check mattered specifically because the gridlines introduced a CSS variable charts had never previously used.

---

## Phase 14 — Tools: audit, then three new tools

✅ Done. After Simulation reached beta-readiness the user asked what else the Tools section genuinely needed, then approved auditing the existing tools first and building all three proposed additions.

**A correction owed up front.** Earlier in this work I called openFDA's Complete Response Letter database a "standout find" for Tools. Verified against the live API: **there is no CRL endpoint.** The drug endpoints are FAERS, SPL labeling, enforcement/recalls, NDC, Drugs@FDA and shortages. The claim was wrong and was retracted before anything was built on it.

**Audit of the 10 existing tools.** Every capability built into the EDGAR/CT.gov/openFDA engines was confirmed already exposed in the UI — no built-but-hidden features. All 10 tabs driven live: 0 console errors, 0 warnings. Two real findings:
- **Unverified math behind Diluted Market Cap.** The aggregate `computeCapitalStructure` had two treasury-method checks, but `ifConvertedShares`, `computeEquityValue` and `applyFutureRaise` had none. The convertible logic has a subtle either/or — the same note must count as *either* equity (shares, leaving net cash) *or* debt (subtracted from net cash), never both, never neither — now tested both ways. The future raise is verified against a closed-form identity rather than implementation output: raising at exactly the current per-share value is value-neutral, since (E+X)/(S + X/P) reduces to E/S when P = E/S. No engine bugs; unproven math now proven.
- **A chart that existed only in the PDF.** The report rendered a "Where This Case Sits Among Real Comps" ranked bar chart that the Peak Sales Comps *tool* never showed, so the best sanity-check on a peak-sales assumption required exporting a PDF. Surfaced in the tool as a shared component. The report's copy was deliberately left unrefactored — that path is verified and print-styled against its own tokens, and this project's conventions keep architecture changes in their own commit.

**A false lead worth recording.** A grep of `chart.js` for `<title>` and tick logic returned zero and looked like the same defect just fixed in `ts_chart.js`. Reading the code disproved it: `RevenueChart` and `ScatterChart` already have crosshair hover tooltips and Y-axis gridline labels, built with React state rather than SVG `<title>` — richer than the Simulation approach, not a defect. Pattern-matching one section onto another is exactly how a false finding ships.

**Tool 1 — Runway vs. Catalyst.** Answers what breaks more retail theses than bad science: even if the drug works, does the company reach the readout without financing first? Both halves already existed and had never been crossed. Three states rather than a binary — funded / tight / gap — because a company cannot raise on fumes, so "technically reaches it" and "funded through it" are different claims. The headline names the *earliest* non-funded catalyst, since that is what forces the raise. **A real bug found live:** `computeForwardRunway` returns `null` both for missing inputs and for a case whose cash flow turns positive before cash runs out; conflating them told a fully funded company (final balance $18.9B) it was misconfigured. Now disambiguated on starting cash, with `beyondHorizon` and a "No end" rendering.

**Tool 2 — Exclusivity / LOE.** Replaces a pure guess (the LOE year feeding the erosion model) with FDA's published patent set. This endpoint had been **deliberately deferred**, with a scope note saying sources conflicted on whether patent data was live-queryable and it was "left out until that's verified directly." It has now been verified directly against the live API, so the condition the deferral set is met. Deliberately shows two dates, not one: Eliquis's substance-patent floor (2026-11) and its last patent to expire (2041-05) are **fifteen years apart**. **A real bug found live:** openFDA signals "no matches" with HTTP 404 and `fdaFetch` throws on any non-2xx, so the informative not-found path was unreachable and a Keytruda lookup reported a bare "HTTP 404". A 404 here is data; now routed so a biologic lookup explains the Purple Book rather than implying the drug has no patents.

**Tool 3 — Binary Event.** Implied probability from three numbers, closed-form. The mirror of the existing full-case implied-PoS solver, which needs a built case; this works as a screen. Out-of-range implied probabilities are reported rather than clamped, because that is the most informative outcome — it means the market disputes one of your anchors, not your odds.

**In-app guide updated in the same pass**, applying the lesson from the Simulation phase where the guide was left stale and had to be caught in a later double-check. All three new tools plus the comp-ranking chart are now documented in Reference Sheet → How This Works. `final_sweep.js` updated to 13 Tools tabs.

`math_verification.js` at **384 checks, 0 failures** (+70 this phase). Full suite clean.

### Phase 14b — Export parity, report de-duplication, and a final UX pass

✅ Done. Closing the two items flagged as open at the end of Phase 14, plus a sweep for bugs and UI snags across both finished sections.

**Export parity.** Simulation had been given an export path on every tool as an explicit earlier fix; Tools never had that pass. Five results panels could not be got out of the app at all: Runway vs. Catalyst, Binary Event, Exclusivity/LOE, the new peak-sales comp-ranking chart, and the Sensitivity tornado. All five now carry export controls.

This needed one small addition to the shared component: `ExportControls` always offered PNG and SVG, both of which work by finding an `<svg>` in the subtree. Several of these panels are built from styled HTML instead (the runway timeline, the comp ranking bars, the tornado), where those two buttons could only ever fail. Added a `panelOnly` mode that hides them and offers panel capture alone, rather than presenting an action guaranteed to error.

**Report de-duplication, done narrowly.** The PDF report kept its own copy of the peak-sales comp chart. Rather than force both through one component — the report needs print-safe colour tokens and tighter sizing, so sharing the rendering would have taken more config props than it saved — only the *selection logic* was extracted into `selectPeakSalesCompWindow`. That is the part that genuinely must not diverge: which comps appear and where the case ranks. Rendering stays separate on purpose. 15 checks cover the window maths including clamping at both ends of the ranking, and an explicit check that the report's `6` and the component's default agree. The report was re-rendered afterwards to confirm the comp section still appears.

**Final UX pass.** All 26 tabs across Tools and Simulation driven live and audited for horizontal page overflow, elements overflowing their box without scroll or ellipsis, and sub-18px tap targets. **Zero console errors, zero console warnings.** Three elements flagged as overflowing turned out to be SVG `<text>` nodes (a rotated axis label, two chart value labels) — SVG text has no CSS box model, so `scrollWidth`/`clientWidth` there is meaningless. Confirmed visually as false positives rather than assumed.

**A process note worth recording.** A scripted edit to wire the tornado's export used a brace-matching heuristic to find the block's close, and it matched the wrong line — producing a syntax error and a stray edit elsewhere in the file. Reverted that one file to HEAD (keeping the good, independent change in `helpers.js`) and redid both edits anchored on the full literal block instead of guessing at closing parens. Heuristic edits into deeply nested `React.createElement` trees are not safe; anchor on complete, unique text.

`math_verification.js` at **399 checks, 0 failures**. Full suite clean.

---

## Phase 15 — Workspace: benchmark verification and two real valuation gaps closed

✅ Done. With Simulation and Tools finished, the user asked for a completeness and accuracy audit of Workspace — the valuation core — against the source documents and outside practice, explicitly deferring the Quick-vs-Detailed restructuring to a separate discussion.

**Benchmark verification: clean.** Spot-checked `data.js` against the figures recorded from the two source documents (Pharmagellan's *Biotech Financial Modeling* as primary, Bay Bridge Bio's primer as secondary): base PoS 60/36/63, nonscientific-adjusted 70/52, regulatory 88.4%, Thomas baseline 63.2/30.7/58.1, regulatory modifiers +9.2/+3.9, discount 10% acquirer / 20% pre-commercial — all match. Three apparent misses (13yr LOE, 6-year launch curve, 50% ex-US price) turned out to be `engine.js` defaults that correctly mirror the sourced values, not gaps.

**A wiring audit of every benchmark constant** — is each one actually used by an engine, or merely displayed? Of 30 constants, 14 are engine-unreferenced, and all but one are correctly reference-only (comps databases surfaced through explicit one-click export, provenance tables, definitional pricing matrices, the placebo benchmarks built as reference by design).

The exception was real: **`POS_BY_MOLECULE` was sourced, displayed, and connected to nothing**, even though every program already carries a modality that drives COGS and erosion. Same class of gap as the session-7 finding where PoS modifiers were sourced but unwired. Now composed as a ratio against `POS_BY_MOLECULE.all` — which is conveniently the identical 63.2/30.7/58.1 triple as `POS_MODIFIERS.baseline`, so both axes share one baseline. The effect is large and compounds across three phases: a biologic runs 1.85× the blended baseline to launch versus 0.94× for a small molecule, roughly a 2× spread. Because it is applied automatically rather than chosen, it is now stated explicitly on the program panel with its ratios and source, so no number moves for invisible reasons. **Cell and gene therapy deliberately get no adjustment** — those sources run 2010-2016 and their "biologic" cohort is antibodies and proteins, so mapping gene therapy onto it because both file as BLAs would be inventing a datapoint, not sourcing one.

**Cash tax with NOL carryforward — the largest genuine gap, and squarely the commercial-biotech direction the user asked about.** `dcfEngine.js` had stated plainly that EBIT proxies FCF with no tax. Defensible pre-revenue; materially overstates a company that is actually selling, and worst precisely where it matters most, since terminal value is typically the majority of a DCF. Implemented as `applyTaxToCalendar`: projected loss years build a shield, profits draw it down, and only income beyond it is taxed. **Off by default** — every case built before this existed assumed pre-tax flows, and defaulting it on would have silently re-priced all of them. Threaded into the headline valuation, the SOTP decomposition, and both cash projections (runway and dilution path), since cash tax is a real outflow in a financing projection too. Two simplifications are stated rather than hidden: it taxes the risk-adjusted flow (taxing unrisked and then risk-adjusting would double-count the probability), and it ignores the 80%-of-income annual cap on post-2017 federal NOL use, which errs mildly optimistic.

Verified on a real case: no tax $3,579M → 21% $2,848M (−20.4%) → 40% $2,187M, and a $5B starting NOL correctly pulls the taxed value back toward the untaxed one.

**Preclinical stage** was identified as a third gap and, per the user's decision, deliberately not built: the primary source covers Phase 1/2/3 only, and adding it would mean importing a different source's transition rates.

**A test-side lesson repeated.** The first run of the new tax checks failed six assertions — all of them a units error in the *test* (the calendar carries dollars; `startingNOLM` is the one field in millions), not the engine. Same pattern recorded in session 8: a verification suite's failures are as likely to be the test's fault as the code's.

`math_verification.js` at **431 checks, 0 failures** (+32 this phase). Full suite clean.

---

## Phase 16 — Quick vs. Detailed: mode separation without losing capability

✅ Done. The user's hypothesis was that the app had two valuation modes — a "napkin" simple-multiple and an institutional book-based DCF — and wanted separation so double-counting and irrelevant inputs were minimised.

**The hypothesis was right in spirit, but the app wasn't built that way.** There are two *independent* toggles at *different scopes*: `revenueMode` per program (Quick/Full — how peak revenue is derived) and `valuationMethod` per case (DCF/Simple Multiple — how peak revenue is valued). The user's mental model is the diagonal of that 2×2.

**Checked the double-counting concern against outside practice before acting, and the app was already right.** Applying PoS *and* discounting on top of a comp multiple is standard, not double-counting; the real risk is combining a probability adjustment with a risk-loaded discount rate, and `data.js` already carries the source's explicit note that its 15–20% range is "compatible with independently risk-adjusting the cash flows." Nothing to fix there.

**The irrelevant-input problem was real and larger than expected.** Read directly from `computeSimpleMultipleValuation` rather than assumed: in Simple Multiple mode the engine reads revenue, years-to-peak, R&D *timeline*, PoS, capital structure, financing, discount rate and the multiple — and reads **nothing** from cost structure, R&D *cost*, corporate G&A, terminal value, or cash tax. All of those were still shown and fully editable. You could tune COGS to 22% in a mode that never looked at it.

**Rejected collapsing to a strict two-mode switch, even after the user confirmed existing cases were irrelevant.** The migration concern was never the real argument. Both off-diagonal combinations are genuine workflows: a trusted consensus peak run through proper cost/timing modelling (Quick + DCF), and a bottoms-up epidemiology build valued off comps rather than modelled costs (Full + Multiple). A strict either/or would delete both.

**What was built instead:**
- **Mode presets** — one-click "Napkin" (typed peak × multiple) and "Full model" (epi build + bottoms-up DCF) that set both axes together, with a "Custom" state that names which hybrid you're in. The mental model becomes the default path without forbidding the other two.
- **Precise input gating**, driven by a single `VALUATION_METHOD_USAGE` table so the UI can't drift from the engine. Cost structure, terminal value and cash tax are hidden in Simple Multiple mode with a note saying what to switch to. R&D is deliberately *not* hidden — its timeline is live (it sets the discount horizon) while its costs are not, so the section stays with the distinction stated.
- **Stage-aware multiple guidance** — 5 of the 7 precedents are post-approval acquisitions, so those multiples price an asset with no clinical risk left. Applying one to a pre-approval program and then risk-adjusting is the correct composition, but a raw post-approval multiple is the wrong *starting point*. The panel now says so at the point of choosing, citing the 1–2× (Phase 2) and 3–5× (Phase 3) unadjusted rules of thumb against the 7.8× precedent median, and explains why the default is 3×.

Verified live in both directions: the Full-model preset restores cost structure, terminal value and tax; the Napkin preset hides all three, keeps the R&D timeline with its cost caveat, and surfaces the stage warning. 431 math checks, full suite clean.

---

## Phase 15 — Beta-readiness overview: the full-project rigor pass

✅ Done. Framed by the user as shipping a beta to VCs, with two stated top priorities: mathematical/statistical accuracy, and every benchmark tracing to the primary source documents.

**Benchmarks re-verified against the ORIGINAL sources, not memory.** Both .docx files were recovered from disk and text-extracted (Pharmagellan *Biotech Financial Modeling*, 42 embedded images; Bay Bridge Bio *Biotech Investing Basics*). Confirmed verbatim against `data.js`: PoS base case 60/36/63 ("we typically assume base-case success probabilities of 60% for Phase 1, 36% for Phase 2, and 63% for Phase 3"), regulatory ~88%, G&A $8.7M median with $6.3–12M IQR, SG&A 34% of revenue stabilizing above $400M, ~13 years launch-to-competition, ASP at 74% of AWP, small-molecule 90% volume loss (88% unit decline at 12mo, 93% above $250M), biologic 60–70% US value lost over 5 years, adherence 85% acute / 70% chronic. Every figure matched. `data.js` also already documented a genuine internal tension in the source's own biologic-erosion numbers rather than papering over it. **No benchmark was changed** — the hard rule holds.

**The biggest math gap found and closed.** A coverage audit showed 81 of 180 engine functions independently verified — and the unverified set included the entire revenue build: the population funnel, launch curve, exclusivity erosion, full program revenue, and the whole cost chain. For a tool whose only output is a valuation, that was the wrong thing to leave untested. Added 76 hand-derived checks. Four initial failures were all the TEST's fault and diagnosing them surfaced something worth pinning: two launch-curve constants exist and `launchCurveForYears` deliberately prefers the source's exact empirical curves (Fig 6-2) over the adapted 6-year one (Fig 6-1), which disagree at 11% vs 10.6% in year 1.

**Five real bugs found and fixed, none of which would surface from reading code:**
- **openFDA drug-label lookup returned "not found" for every drug ever searched.** `URLSearchParams` encodes `+` as `%2B`, which openFDA reads as a literal plus rather than a clause separator. Confirmed against the live API in both directions before fixing.
- **The EDGAR cache was unbounded by bytes** (200-entry cap, but responses are megabytes) and had reached 11.9MB — at or past a typical 5MB quota, meaning routine case saves were quietly running their failure path.
- **Enabling Dilution Path silently discarded a manually configured future raise.** Worse than double-counting: the user sees a filled-in field having no effect. Verified empirically (60M → 50M shares) before and after.
- **Simulation crashed on handler-after-unmount**, in three successive layers, only visible as each was fixed. It is the app's one vanilla-DOM section with no error boundary above it.
- **A stale identifier broke the Exclusivity tool** — caught live, and worth noting the error boundary degraded that one view instead of white-screening the app.

**Data-safety layer built:** confirmation on all seven previously-unprotected destructive actions (inline two-step for list items, modal for whole cases/programs), byte-capped caches, and a proactive storage banner that splits user data from disposable caches and offers the safe remedy inline.

**PDF made comprehensive:** the report covered Workspace only. Simulation and Tools results are computed on demand and have no persistent model to re-render, so "Pin to report" captures the rendered panel and attaches it to a case — downscaled and capped at 12 per case, since these persist beside the user's cases.

**Beginner guidance completed:** audited rather than assumed — Simulation had 10 of 12 covered, Tools 3 of 13 (two of which only appeared *after* a result, which is backwards). All now carry an optional collapsed disclosure.

**All nine external APIs live-verified** end to end. **All 90 buttons** across all five views click cleanly. **Both themes** verified with zero errors, zero warnings, and no invisible text.

`math_verification.js` at **568 checks**, full 10-file suite clean.

---

## Phase 16 — Rename to RxNPV, public GitHub, and the identity scrub

✅ Done. The app's name at the time of Phase 15 was tied to a pseudonym the user had previously used online. Having since moved to operating under their real identity on a new GitHub account (`risdiplam`), the user asked for a full rename to **RxNPV**, then to make the repository public, which raised the bar from "rename the files" to "make sure the old identity cannot be found anywhere in this project, including in git history."

**The rename itself.** Every occurrence of the old name replaced across source, build config (Electron `productName`/`appId`/window title), the in-app wordmark (restyled two-tone treatment, same convention as before), doc filenames, and the PDF report footer. One thing this touched that needed real care rather than a blind find-and-replace: `electron/main.js` carried a one-time `userData` migration function from an even earlier project name — Electron derives the save-data directory from the app name, so a bare rename silently orphans every existing case. Checked directly against disk rather than assumed which directory actually held live data (it was not the one the naive single-name migration logic expected), fixed the migration to check both legacy names in the right order, then — once the migration was confirmed live and successful — removed the function entirely rather than leave old identity-linked directory names sitting in source that was about to go public.

**Going public changed the bar from "renamed" to "scrubbed."** A full repo-wide sweep confirmed zero remaining traces of either the pseudonym-linked name or the project's original name, in any tracked file or filename — including a second, broader pass after the user asked for every mention of the original project name to be cleared too, not just the pseudonym-linked one. Separately verified no personal name, real email address, or local machine username had ever leaked into tracked source, comments, or the built app's own metadata (checked the packaged `.app`'s actual `Info.plist` directly rather than assumed the generic `productName` config was the whole story).

**The part file content alone couldn't fix: git history.** Every one of the ~59 commits made across this project's Claude Code sessions had a message describing work by the old names, often many times per message. Renaming files today does nothing to erase that — a public repo's commit history is effectively permanent once visible, so this needed a real decision, not a code change. The user chose: public repo, full history discarded. The entire history was bundled to a local backup file (kept entirely outside git, never pushed anywhere) before being irreversibly squashed into one clean initial commit and the old commit objects actually garbage-collected out of the local repository (`.git` shrank from 11MB to 1.1MB, confirmed zero unreachable objects afterward) — not just hidden behind a new branch pointer.

**GitHub setup, walked through step by step for a non-developer.** The user does not use git/GitHub day to day, so this was done as an explicit, no-jargon-assumed walkthrough rather than a list of commands: opening Terminal, creating the repo on GitHub.com, generating a classic Personal Access Token, and completing the first authenticated push themselves (never Claude — entering credentials/tokens is off-limits regardless of who hands them over). One real moment worth recording: the user pasted a live token directly into the conversation. It was not used, the user was told to revoke it immediately since a chat transcript is not a secure channel for a live secret, and a fresh token was generated and used correctly the second time. Also worth recording: GitHub's actual settings navigation didn't match what was expected from training-data-era knowledge of their UI (no "Developer settings" where anticipated) — rather than insist on a remembered path, gave the user a direct, stable URL (`github.com/settings/tokens/new`) instead of describing sidebar navigation that had evidently since moved.

**Local backups, verified restorable, not just created.** Two independent backups made after the push — a git bundle (full repo history, portable, needs git to restore) and a plain zip of the source tree (needs no git knowledge at all) — each one test-restored into a scratch location and diffed/inspected before being trusted, per this project's own "verify before considering it done" standard applied to the git/backup work itself, not just application code.

**A standing instruction now written into this file's header**, not just agreed to conversationally: work continues locally on this Mac (a real Mac is required to build/package/verify a macOS-only Electron app), but every meaningful unit of work is committed and pushed to GitHub before moving to the next thing, rather than batched into occasional save sessions — directly addressing the user's stated fear of catastrophic local-only data loss.

Verified end to end after every step in this phase: full rebuild, full 10-file test suite (568 math checks, zero functional errors), the packaged `.app` reinstalled and relaunched, and the real "Stok Therapeutics" case confirmed intact — through the rename, through the folder rename the user did independently afterward, and through the final cleanup of old artifacts.

---

## Phase 17 — The results reader: what the trial actually reported

✅ Done. The biggest remaining hole in the app, identified while writing `RxNPV_Feature_Map.md`: the single most important moment for an investor is the readout, and until now the app's answer to it was a hyperlink. `ctgovEngine.js` parsed the protocol and deliberately stopped at `hasResults: true` — the right call when nothing here could read a results section safely. The analog effect-size board changed that; it already reads the same schema, narrowly and with stated denominators, and this extends exactly that discipline.

New module `src/trialResults.js` (pure, no network, no DOM), folded into the **Trial Decoder** rather than given a tab of its own — same input, and putting it below the design cards makes the before/after framing explicit rather than optional. It costs no extra API call: CT.gov's single-study endpoint already returns `resultsSection` in the response the decoder was fetching anyway, so `fetchStudyByNctId()` now returns both. The results are deliberately *not* folded into `parseStudy()`, because that shape is what the Trial Watch persists to `localStorage` and a snapshot has no business carrying a 259-row adverse-event table.

**Three panels.** *Outcomes* — the registered endpoints with their per-arm values and the sponsor's own registered analysis: effect estimate, confidence interval, p-value, statistical method, and whether the comparison was **superiority or non-inferiority**, surfaced rather than buried because reading a non-inferiority result as a superiority claim is one of the most common ways a press release gets over-read. *Participant flow* — who finished and who left, by arm; differential dropout is the single most important thing the decoder structurally cannot see before results exist. *Adverse events* — for an unapproved drug this is the only real safety data that exists anywhere, since FAERS has no denominator and a label does not exist yet.

**The honesty rules, which are the whole reason this was safe to build:**

- **Nothing infers which arm is the drug.** CT.gov does not state it reliably, so groups are reported under the sponsor's own titles and every difference is signed and explained, never editorialised as good or bad.
- **A measure reported across strata or categories is never collapsed into one number.** Real records go up to 56 classes. Only a single-class, single-category measure is a plain "this arm got this number"; anything else says how it was reported and points at the record.
- **Deaths are never counted as dropout**, and neither is moving into an open-label extension.
- **A reason the sponsor never registered reads as "not registered", not 0.0%.** KEYNOTE-189 files no "Adverse Event" withdrawal row at all; printing 0.0% would turn a gap in the record into a clean tolerability result.
- **Every rate carries its denominator**, the safety denominators are labelled as the safety population rather than the randomised population, and the registered frequency threshold is stated so the non-serious event list reads as a floor rather than a census.

**Two bugs only live data could have found**, both of the same kind the project has hit before — a fixture encodes the same wrong assumption as the code, so unit tests pass while the feature is wrong:

1. **"Not completed" is not the same as "left".** NCT04368728 registers ~44,000 participants whose reason for not completing the blinded period is *"Participants entered open label period"*. Counted as attrition, that reads as **99.8% dropout on a trial whose real dropout was about 2%**. Fixed with a transition bucket alongside the death bucket.
2. **A results record's event groups are not the randomised arms.** KEYNOTE-189 registers five, three of them crossover and re-treatment cohorts — one with nine patients, one with two. Ranking events by the highest rate across all five put a 2-of-9 event at the top of a table where every visible column read 0.0%. Ranking and display now both use the groups with real denominators (anything under a tenth of the largest is excluded), so a nine-patient cohort cannot decide what the top row is.

A third, smaller one: an exactly-ten-point gap between two floating-point rates evaluates to `0.09999999999999999`, so a literal `>= 0.10` silently declined to fire on the clean case a threshold exists for. Every threshold comparison now goes through an epsilon.

**Results red flags** are a third, separate set — distinct from `decodeTrialRedFlags()` (the registered protocol) and `computeRedFlags()` (the user's own modelling inputs), and the UI says so. They cover: a primary registered but not posted, no between-group analysis registered for the primary at all, a primary interval that spans no-effect, a non-inferiority comparison, differential dropout ≥10 points between arms, ≥20% attrition in any arm, an AE-withdrawal gap ≥5 points, a serious-AE or death-rate gap between two arms, and results posted more than fifteen months after primary completion. Identical flags are stated once — a master protocol can otherwise raise the same sentence sixty times from one record.

**Verified**: `math_verification.js` from 724 to **793 checks**, all hand-derived, including regression tests built from the exact records that produced both live bugs. Full 10-file suite clean. Driven live in the packaged-equivalent Electron build against four real records — KEYNOTE-189, DAPA-HF, DECIDE and the 64-primary-endpoint BNT162b2 mega-protocol — plus a trial with no results, with **zero console errors** in every case and the rendering checked by eye in both a screenshot and the text tree.

---

## Phase 18 — Gross-to-net: the advice the app gave and had no way to follow

✅ Done. Not a new feature — an accuracy defect, found while writing `RxNPV_Feature_Map.md`. The Reference Sheet has always said the right thing (*"ASP — net of rebates and discounts, use this in models. Median ~74% of AWP"*), and the revenue model had exactly one price field, no basis attached to it, and nothing that noticed a list price entered into it. In US pharma that is one of the largest single sources of error in a retail revenue model: entering AWP where ASP belongs overstates US revenue by 1/0.74 — **35%** — all the way through peak revenue, the DCF and the per-share number, silently.

**What was built.** The price field now carries a **basis** (ASP / WAC / AWP / Retail) and the revenue build converts to ASP before multiplying by patients. The conversion uses the app's own sourced interconversion table (Table 4-1) rather than an invented gross-to-net percentage — ASP is 74% of AWP, 79% of Retail, 88% of WAC — so the number doing the work is one the Reference Sheet already displays and cites. An optional **net price realisation** override beats the table for anyone with a real figure, and the editor shows the conversion as it will actually be applied (`$200,000 on an AWP basis → $148,000 net per patient-year, a 26% gross-to-net deduction`), because a basis control that silently moved the valuation would be worse than not having one.

**Backward compatibility was the binding constraint, not an afterthought.** The user has real saved cases. The default is an ASP basis with no adjustment and a blank override, and `getRevenueBuild()` now merges pricing defaults rather than taking the saved object wholesale, so a case saved before any of this existed values to the cent as it did before. That is asserted directly in the test suite, both on the resolver and end to end through `computeProgramRevenue`.

**Two deliberate boundaries.** Ex-US revenue still prices off the *entered* figure, not the US net price: Table 4-2's cross-country factors compare list prices, so applying them to a netted US price would discount twice. Ex-US markets have their own, much smaller gross-to-net, and this does not claim to model it — the editor says so on screen. And Quick mode is untouched, because a stated peak revenue is already a revenue number with nothing to net down.

**One new red flag, deliberately narrow.** Table 4-1's figures are averages across every drug in the source and understate modern US gross-to-net badly — 40–50% deductions off list are ordinary for a specialty or rare-disease brand. So a case that enters a *list* price and leaves the conversion on the table default gets a low-severity flag saying exactly that. Entering your own realisation figure — the intended resolution — stops it. A case on an ASP basis never sees it, because that would fire on every case ever built and mean nothing.

**Verified**: `math_verification.js` from 793 to **818 checks**, every expected value worked out longhand, including the 1/0.74 overstatement ratio stated as its own assertion and the legacy-case-values-identically invariant. Full 10-file suite clean. Driven live in Electron: basis selector switches, the conversion line updates, the override takes precedence, the red flag appears on the Workspace, **zero console errors**.

---

## Phase 19 — Two cheap completions: what a change means, and where a number sits

✅ Done. Both are small additions to machinery that already existed, and both close a gap where the app was collecting the right data and then saying too little about it.

**Trial Watch now says what a change means.** The snapshot diff covered five fields and treated all of them alike, so "recruiting → active, not recruiting" and "the primary endpoint was replaced" arrived as the same kind of event. They are not remotely the same event: a protocol amendment after a trial is underway is one of the very few things a retail investor can see *before* a readout that genuinely changes what the readout will mean.

It now diffs thirteen fields *(this line first said fourteen; five existing plus the eight below is thirteen, and the September 2026 audit caught the slip after it had been copied into three other docs)* — adding allocation, masking, arm count, arm labels, eligibility criteria, secondary endpoints, completion date and why-stopped — and ranks every change into one of three bands: **high** (changes what the trial can establish: the endpoint replaced, the blind coming off, randomisation changed, enrolment cut by a fifth or more, the trial stopped), **medium** (changes the terms — timing, size, who is in it), **routine**. The list sorts by band so the amendment is read before the housekeeping.

Two judgement calls worth recording. *Eligibility* is reported as changed and by how many characters, never interpreted — whether an amendment widened or narrowed who can enrol is genuinely not inferable from a text diff, and a tool that implied otherwise would be worse than one that says nothing. *A baseline saved before a field existed* is skipped rather than diffed: a snapshot taken before the decoder work has `undefined` for masking, and reporting `undefined → DOUBLE` as a protocol amendment would be a fabricated finding. The app detects that case and tells the user which three fields are not being compared this time and why.

**The analog effect-size board can now place a number in its own distribution.** The board answers "what got posted in this indication"; this answers the question a reader has half a second later, in both directions. Before a readout: a modelled hazard ratio of 0.62 where the posted results run 0.78 to 0.91 is a claim about beating the field, and should be seen as one. After a readout: a hazard ratio of 0.91 that cleared its p-value is a statistically real, clinically thin win — and saying so as a **percentile of the actual reference class** is the right way to do it, because no fixed "thin win" threshold could work across indications. A thin effect in oncology and in a rare metabolic disease are not the same number, and nothing hard-coded could know which one you are in.

It compares only within a scale — a hazard ratio against hazard ratios, never against a mean difference — reports the rank with its denominator rather than a verdict, and states the one thing a percentile cannot tell you: a point estimate can sit high in the ranking and still have an interval consistent with no effect.

**Verified**: `math_verification.js` from 818 to **852 checks**, including the phantom-change guard and both scale directions (lower is better on a ratio, higher on a difference). Full 10-file suite clean. Both driven live in Electron — the watch against a deliberately stale baseline planted for DAPA-HF, so the diff had a real endpoint swap, arm change, enrolment cut and status flip to rank; the board against a live NSCLC Phase 2 query. **Zero console errors.**

---

## Phase 20 — Form 4: the option grants, and two bugs that were hiding behind them

✅ Done. The documented gap was narrow — derivative transactions (option grants, RSU vesting) were never parsed, so a panel could print "no transactions found" in a week the CEO received a multi-million-dollar award. Closing it turned up two larger problems that had nothing to do with derivatives and everything to do with only ever having been checked against parsing logic rather than against the live service.

**Bug one: the filings being read were an arbitrary sample.** Discovery went through EDGAR's full-text search, which returns hits in **relevance order, not date order** — confirmed live: asking it for Sarepta's Form 4s returned filings from 2008, 2011 and 2016 among the first ten, out of 277 total. The panel said "the last 20 Form 4 filings" and was showing twenty essentially random ones, which means a company's most recent insider activity could be entirely absent from the one panel whose job is to show it. Discovery now comes from `data.sec.gov/submissions/CIK….json`, whose `filings.recent` arrays really are reverse-chronological, which this file already fetched and cached for other purposes, and which needs no second API surface. Same company, after: September 2026, July 2026, June 2026.

**Bug two: officers were being labelled "Other".** The relationship flags in a Form 4 are booleans, and filers write them both ways — the schema's own examples use `1`/`0`, real filings routinely use `true`/`false`. The code checked only for `"1"`, so a live filing from Sarepta's **Chief Financial Officer** came through as role "Other". Who was transacting is most of what an insider transaction means. Both spellings are now accepted, and officer is checked before director, since a filer who is both is more usefully described by their executive title than by a board seat.

**The derivative table itself** is now parsed: security title, transaction code, share count, strike price, exercisable-from and expiry dates, and the underlying security and share count read separately rather than assumed 1:1. No dollar value is computed for a grant — a grant's registered price is normally zero, and a notional built from a market price this tool does not have would be a made-up number.

**And the split that actually matters.** Reading both tables is only half the job; presenting them together would have been worse than not reading them at all. A restricted-stock award is filed in the **non-derivative** table, so splitting the display by derivative/non-derivative would have put a CEO's 756,104-share grant under "bought and sold" — the exact confusion this was meant to prevent. The tabs therefore split by **what the transaction is**: only codes P and S (an insider deciding to buy or sell at a market price with their own money) count as bought & sold; awards, vesting, tax withholdings, gifts, conversions and option grants all sit on the other tab. The headline summary counts P and S only, because a "net insider buying" figure that folds in grants and withholdings is how that figure stops meaning anything.

Live, on Sarepta: **1** open-market transaction (a director selling 7,239 shares) against **22** awards, vestings and withholdings — including a 2,224,342-share option grant to the incoming CEO at a $17.74 strike, which the app previously could not see at all. The old panel would have reported twenty-two "insider transactions".

**Verified**: `math_verification.js` from 852 to **865 checks**, asserting that a grant is not counted as buying, a tax withholding is not counted as selling, an empty filing set reads as nothing-happened rather than zero-net, and repeat transactions by one insider count as one buyer. Full 10-file suite clean. Driven live in Electron against Sarepta, Moderna, Vertex, Alnylam, BioMarin and Regeneron. **Zero console errors.**

---

## Phase 21 — The literature shelf, and why the earlier decline was wrong

✅ Done. This was previously declined as "duplicating Google Scholar", and that was the wrong read for one specific reason: **a general search engine cannot tell you what kind of paper it just handed you.** Europe PMC returns MEDLINE's own publication types on every record, so a primary randomised trial report, a meta-analysis, a narrative review, a conference abstract and an unreviewed preprint arrive already separated — and the difference between "three trials support this" and "three reviews of the same trial support this" is most of what someone building a view is trying to establish.

**On the name, which misled the scoping conversation.** The user's condition for building this was "so long as we also have others — can't be just the European one." That turned out not to be a constraint, and the honest answer was to say so rather than bolt on a second API to satisfy a misunderstanding. Europe PMC is not a European literature database: its `MED` source is MEDLINE/PubMed **in full**, the same index PubMed searches, plus PMC open-access full text, plus preprints from bioRxiv, medRxiv and Research Square. Verified live before saying it: a broad query returns 96 of 100 records from `MED`, NEJM and JAMA articles are present, preprints come back under `SRC:PPR`. A second general literature index would return the same MEDLINE records again under a different name.

**Two entry points.** A standalone **Literature** tab for free-text search on a drug, target or indication; and a **"what has been published about this trial"** panel inside the Trial Decoder, which searches the NCT number. The panel is a separate, explicit call — a trial lookup should not silently become two round trips when the registered design is often all a reader wanted.

**The composition bar is the point**, not the list. "4 primary papers · 15 reviews · 1 case report · 5 papers of unstated type" tells you the shape of an evidence base in one line; a list of twenty-five titles does not.

**A live finding that changed a default.** Relevance ranking is the wrong default here. On `sotatercept pulmonary arterial hypertension`, best-match returned four 2026 letters and editorials with zero citations at the top, and the trial everyone is actually arguing about nowhere in sight. Sorting by citations puts STELLAR (Phase 3, NEJM 2023, 590 citations) first and PULSAR (Phase 2, 496) second. Most-cited is therefore the default, with best-match and most-recent still available — and the citation count is labelled everywhere as the age-biased popularity measure it is, useful for finding the paper everyone cites and useless as a quality score.

**Three classification rules worth recording.** A preprint is a preprint whatever else its record claims — an unreviewed manuscript describing an RCT is still unreviewed, so source `PPR` outranks any type label on the record. A **conference abstract** gets its own tier rather than passing as a paper, because plenty of biotech news cycles run entirely on a few hundred words with no methods section. And a record typed only "Journal Article" is shown as "paper of unstated type" rather than silently bucketed with real evidence — MEDLINE has simply not said what kind of study it is, which is different from it being nothing.

**CSP note**, per this project's own rule about it: `connect-src` gained `https://www.ebi.ac.uk`. jsdom does not enforce CSP, so this was verified in the packaged-equivalent Electron build — the app renders, the search returns results, **zero console errors**.

**Verified**: `math_verification.js` from 865 to **892 checks**, including the real four-type `pubTypeList` from the KEYNOTE-189 record (a randomised-trial tag must beat the plain "Journal Article" on the same record), the decode-then-strip order for MEDLINE's escaped markup, and a preprint keeping its status against a contradicting type label. Full 10-file suite clean. Driven live in Electron against sotatercept (free-text) and NCT04576988 / STELLAR (from the Decoder), which returns its own NEJM report as the top result.

---

## Phase 22 — One asset, every trial — and the evidence-thinness checklist that falls out of it

✅ Done, and these were two items on the list that turned out to be one feature. The app could already search trials, decode one, watch one and read one's results — all of it one NCT number at a time. But nobody holds a thesis about a trial; they hold one about an asset, and an asset is eight to forty trials across different sponsors, phases, indications and fates. Three things are invisible when you read them one at a time: how much of the programme is randomised rather than single-arm, whether this is one focused indication or a platform being tried everywhere, and which trials were quietly stopped.

**The evidence-thinness indicator is the top panel of this view**, which is why it stopped being a separate question. It reports *"18 of 37 trials with a registered allocation are randomised · 11 of 37 blinded · 12 of 40 register a comparator arm · 15 of 40 have posted results · largest single trial 815 participants · furthest reached Phase 4"* — a set of counts, each with its own denominator, and **no composite score anywhere**. A single "evidence strength" number would need invented weights and a reader would anchor on it instead of on the facts underneath; the checklist form is the point. There is a test asserting no line contains a score.

One denominator choice worth recording: the randomised count is stated against *trials that registered an allocation*, not against all of them. A trial that never registered one is not evidence that it was single-arm, and folding it into either bucket would be a guess.

**Trials that stopped get their own panel, with the registered reason.** Live on sotatercept, this is the panel that earns the feature: seven stopped trials, whose reasons range from *"Business objectives have changed"* and *"Persistent low enrollment"* to a terminated **Phase 3** stopped *"due to loss of clinical equipoise based on robust evidence of clinical benefit of sotatercept demonstrated in previous studies"* — a termination that is unambiguously good news. The panel says plainly that sponsors stop trials for business reasons about as often as scientific ones and that the two read very differently, because the count alone would mislead in both directions.

**Two honesty mechanics.** CT.gov's `query.intr` is a loose text search — asking for pembrolizumab returns single-arm nivolumab studies that merely mention it — so every hit is re-checked against its own registered intervention names, including `otherNames` where brand names live, and the drops are **counted on screen** ("3 search hits dropped for not listing this intervention") rather than filtered silently. And the registered-condition list is shown with a note that these are the sponsors' own strings, not a normalised vocabulary, so the same disease appears two or three ways and inflates the count.

**A fourth API lesson, and the sharpest one yet.** The unfiltered response for a 50-trial programme is ~2.8MB against ~97KB filtered, so this query passes a `fields` list. **CT.gov v2 drops `hasResults` entirely when `fields` is specified and it is not requested** — and `parseStudy` coerces the missing value to `false`. The result was not a blank or a zero: the checklist stated, in a full sentence, *"No trial has posted results to the registry yet, so nothing here has a number attached that can be checked against a press release"* — for a drug with fifteen that had. A wrong number is bad; a confident wrong sentence is worse. The field list is now a named constant with a test asserting every field the view reads is in it.

**Verified**: `math_verification.js` from 892 to **939 checks**, including the loose-match filter (a nivolumab study must not count toward a pembrolizumab programme), brand-name matching through `otherNames`, the allocation denominator, deduplication, and the no-score assertion on the description. Full 10-file suite clean. Driven live in Electron against sotatercept — 40 matched trials, 3 dropped, 33 indications, 16 sponsors, 7 stopped. **Zero console errors.**

---

## Phase 23 — Assumption stress under every sample-size result

✅ Done, and the last of the cheap-completions batch. A sample-size calculation is a statement about a world that has not happened yet, and the two inputs most often wrong are *not* the effect size everyone argues about — they are how the **control arm** behaves and how many people **leave**. Both are routinely lifted from a decade-old trial in a differently selected population, and standard of care has usually moved since.

This is a sweep over machinery that already existed — the same verified closed-form power functions the calculation above it uses — so the new content is the reasoning about how the numbers move together, not a new method.

**The part worth building it for: there are two coherent answers and the tool refuses to pick.** If the control arm responds better or worse than planned, what does the treatment arm do? Holding the *absolute* benefit fixed (the drug adds the same number of percentage points) and holding the *relative* benefit fixed (the drug multiplies the control rate) are both defensible, and they disagree enormously. Live, on a trial planned at 30% vs 45% with 163 per arm: if the control arm comes in at 18% instead of 30%, the constant-absolute reading puts power *up* at 87.8%, and the constant-relative reading puts it *down* at 49.4%. Same trial, same miss, opposite conclusions. Which is right is a judgement about the mechanism, not a statistical one, so both columns are shown side by side and the note says plainly that neither is a safe default.

For a **continuous** endpoint the control mean cancels out of the comparison and the variance does not, so the stress is over the assumed SD — an SD a third larger than a small pilot suggested is one of the most ordinary reasons a well-designed trial misses. For **time-to-event** there is no control-rate axis at all, and the panel says why: Schoenfeld is driven by events, so a slower-than-expected control event rate does not weaken the trial, it delays it — a timeline question, not a power one.

**The dropout table states the inflation the right way round.** To keep N analysable after losing a fraction *d*, randomise N/(1−d), not N×(1+d). At 20% dropout that is 25% more patients, not 20%, and getting it backwards is a standard route to a trial that is slightly underpowered by design. The note also says the thing the table cannot model: this treats dropout as random, and when it is not — when the arms lose different kinds of people — the problem is bias in what remains, which no amount of extra enrolment fixes and which the results reader checks for once results exist.

**Verified**: `math_verification.js` from 939 to **964 checks**. The end-to-end case is worked longhand in its own comment — 165 per arm at 30% vs 45% gives z = 0.8647 and power 0.8064; 20% dropout leaves 132 per arm, z = 0.5640 and power 0.7136 — and both are asserted against the hand arithmetic rather than against the app's own output. Also tested: both projection models reproduce the plan exactly at the planned control rate, a relative projection past 100% is clamped and flagged, a failing cell renders as null rather than killing the table, and randomising 207 really does leave 165 after 20% dropout. Full 10-file suite clean. All three endpoint types driven live in Electron with **zero console errors**.

---

## Phase 24 — The Commercial workbench, and a scoping assumption that was wrong

✅ Done. The app assumed pre-revenue throughout. A user modelling an early-commercial name asks a different question — *is this launch actually tracking* — and had nothing to answer it with: the launch-curve benchmarks existed only as forward assumptions, with no way to check a real ramp against them.

**The scoping assumption was wrong, and checking it changed the decision.** This was provisionally scoped as probably-useless on the grounds that CMS data lags one to two years. Checked against the live catalogue rather than assumed: the *annual* datasets do lag that much (2024 data as of September 2026), but CMS also publishes **quarterly** Part D and Part B spending, and that was current to **2026 Q1, published 2026-07-23** — roughly one quarter behind. That is a live read on uptake rather than a historical analog source, and it is the only free, current, drug-level one that exists.

**Launch tracker.** Brand in, and the annual history and the recent quarters are merged into one ordered series with like-for-like growth. Up to three analogs can be compared, indexed to each drug's first Medicare year. Live on Winrevair: 2024 $177.6M across 1,923 beneficiaries → 2025 $499.4M across 4,042 (+181%) → 2026 Q1 $179.2M, an implied $716.7M run rate.

**Actual vs modelled.** Reported product revenue entered per period, persisted on the case, put next to the case's own projection — rebuilt through the same `aggregateCompanyRevenue` path the Workspace uses, so the numbers being compared are the ones the valuation used rather than a second derivation that could drift. Since the model's calendar is deliberately relative, the tool asks which real year is Year 0 rather than guessing, and a reported year outside the projection window is reported as *outside the model* rather than as a 100% shortfall — that almost always means Year 0 is set wrong, and calling it a miss sends the reader after the wrong problem.

**The rule both tools share.** A partial period is never compared to a full one. Three quarters of reported revenue against a full modelled year shows a **25% miss on a drug that is exactly on plan**; a single quarter against a rolling full year shows a **64% collapse in a drug that is tripling**. Both tools compute a like-for-like figure only when the periods cover the same number of quarters, and otherwise compare on a run rate that is labelled as resting on an assumption. Neither tool issues a verdict: a gap against your own model is a statement about the model as much as about the drug, and a launch beating a conservative model looks identical to one beating a realistic model from here.

**A fifth API lesson, and a nasty one.** CMS appends a footnote asterisk to some brand names **inconsistently between its own datasets** — selexipag is `Uptravi` in the quarterly file and `Uptravi*` in the annual one — and the API filter is exact-match. The result was not an error: the tool returned the recent quarters and silently dropped the entire annual history, which on the analog chart left a mature drug's plateau sitting exactly where a new drug's ramp belongs. That reads backwards, not merely imprecise. Names are now compared with the marker stripped, the fetch retries with `name + "*"`, and the marker is stripped again for display.

**A related honesty mechanic that came out of the same bug.** An analog whose first figure is the dataset's own first year was almost certainly selling before it, so indexing it as though year one were its launch year is wrong in the same direction. The engine now knows where each dataset begins — read from every year column present, including the blank ones — and flags such a series in the legend and in a note under the chart.

**CSP note**: `connect-src` gained `https://data.cms.gov`. Verified in Electron, since jsdom does not enforce CSP.

**Verified**: `math_verification.js` from 964 to **1,030 checks**, including the exact Winrevair shape (a Q1 against a full prior year must produce *no* growth figure rather than a false collapse), the three-quarters-annualised comparison that turns a naive 20% miss into a real 6.7% beat, the asterisk normalisation, the dataset-start-year detection, and a gap year rendering as a break rather than a zero. Full 10-file suite clean. Both sub-tools driven live in Electron against Winrevair, Opsumit and Uptravi. **Zero console errors.**

---

## Phase 25 — Six workbenches instead of seventeen tabs

✅ Done, confirmed by the user before building since it changes an interface in daily use.

Tools had grown to seventeen flat tabs across three groups — *Benchmarks*, *Your case*, *Live research* — and those groups were organised by **how each tool worked**: whether it read a static table, computed against case data, or hit an external API. That is an implementation detail. Nobody sits down thinking "I need a tool that hits an external API."

Six workbenches replace them, organised by the **question being asked**, each holding two to four tools that are usually used together and can still be used alone:

| Workbench | The question | Tools |
|---|---|---|
| **Trial** | What is this trial, what can it prove, and what did it report? | Trial Decoder · Asset Program · Trial Explorer · FDA Lookup |
| **Science** | Is the target real, and what has been published about it? | Target Dossier · Literature |
| **Company** | Can this company reach its next catalyst, and who is buying or selling it? | Company Lookup · Catalyst Calendar · Cash Runway · Runway vs. Catalyst |
| **Commercial** | It is already selling — is the launch tracking, and when does it end? | Launch & Actuals · Exclusivity / LOE |
| **Valuation** | What is the case worth, and what is it most sensitive to? | Sensitivity · Binary Event · Diluted Market Cap |
| **Benchmarks** | What have comparable deals and drugs actually done? | M&A Premium · Peak Sales Comps · Licensing Comps |

A sixth workbench — **Valuation** — was added beyond the five the feature map originally proposed, because Sensitivity, Binary Event and Diluted Market Cap answer a question none of the other five do and would otherwise have been scattered.

**The mechanic that keeps this safe.** The `tab` state is still the **tool** id, and the workbench is derived from it (`workbenchForTool`). Every deep link in the app addresses a tool — the external one from Partnership Economics → Licensing Comps, and the internal ones from Company Lookup → "Watch this trial" and Asset Program → "Decode this trial" — so all of them keep working untouched and none can drift out of sync with the grouping. Tool **labels** were kept as they were for the same reason in the other direction: the grouping changed, the vocabulary did not, so nothing anyone already knows the name of has to be relearned. Only "Launch & Actuals" is genuinely new.

**One real consequence, found by the tests.** A tool button now only exists in the DOM once its workbench is open. Four storage-warning tests clicked a tool button directly and broke — correctly, because that is a real change in how the interface works, not a test artefact. They now select the workbench first, which is what a user does too.

**Verification, and an honest note about how.** `final_sweep.js` was rewritten to open every workbench and then every tool inside it, so a tool that exists in the render chain but was left out of a workbench (or put in two) surfaces as an unreachable button rather than as silence — all 6 workbenches and 18 tools visited, zero errors. The deep link was driven live in Electron end to end (Workspace → enable Partnership → click the Licensing Comps link → both the workbench and the tool come back marked `aria-current="page"` with the licensing panel rendered).

**CDP screenshot capture was unavailable this session** — `Page.captureScreenshot` hung, most likely because the Electron window was occluded behind other applications, which blocks Chromium compositing on macOS. Rather than claim a visual check that did not happen, the layout was verified **geometrically** instead: every workbench and tool button's `getBoundingClientRect()` read at 1280, 900 and 700 pixels wide, asserting no overlaps, nothing outside the container, nothing clipped below a legible size, the workbench row staying on one line, and `scrollWidth === clientWidth` so there is no horizontal page scroll. For a change that is two rows of buttons, that is arguably a stronger check than eyeballing a screenshot — but it is a different claim, and it is recorded as one.

---

## Phase 26 — Installed, then looked at it properly, and found two things

✅ Done. The packaged build was installed over `/Applications/RxNPV.app` (the running copy quit first, gracefully). The user's real case survived the bundle replacement intact — checked by launching the installed app and reading the window, not assumed, since `userData` lives outside the bundle but that is the kind of thing worth confirming rather than reasoning about.

**The screenshot problem from Phase 25 had a cause and a fix.** `Page.captureScreenshot` was hanging because the Electron window sat on **another macOS Space** — Chromium's compositor never produces a frame for an occluded window, and `fromSurface: false` does not help. Two things work: the computer-use `app_screenshot` tool captures an off-Space window fine (but **cannot click** it, and `app_bring_to_current_space` refuses outright when the user's current Space is a full-screen app), and `Page.captureScreenshot({ captureBeyondViewport: true, clip })` renders off the compositor path and works regardless. The second is the one to reach for, because it still allows interaction. Recorded in CLAUDE.md so it is not rediscovered the hard way.

**Then the visual check earned itself twice over**, which is the point of doing it:

1. **A results analysis can register fewer arms than it compares.** DAPA-HF files its primary hazard ratio against `groupIds: ["OG001"]` — a single arm, for a two-arm comparison, with no `estimateComment` either. The panel rendered the comparison line as a bare **"Placebo"**, which reads as though an HR of 0.74 ran *against* nothing, or as though placebo were the numerator — on a trial where dapagliflozin is plainly the favoured arm. No test caught it because the engine was behaving correctly: it passed the sponsor's list through untouched, exactly as designed. The defect was entirely in what the UI did with a one-element list. It now prints a comparison line only when both sides are named, and otherwise says the record does not state which arms the estimate runs between or which way round, pointing the reader at the per-arm counts instead.

2. **The same missing-data condition was rendered two different ways in adjacent columns.** In the participant-flow table a reason the sponsor never registered showed as "not registered" under *Left for an AE* and as a bare em-dash under *Deaths* — identical underlying state, two different readings, and the dash is the one that looks like a zero. Both now read "not registered".

Neither would have surfaced from the text tree, the geometry check, or any assertion in the suite. Both came from looking at the rendered page.

**Verified**: `math_verification.js` **1,032 checks** (two added, asserting that an analysis naming one arm keeps exactly one and an analysis naming none reports none, rather than either being backfilled). Full 10-file suite clean. Re-driven live against NCT03036124 with the corrected copy on screen, zero console errors. Packaged, installed, and byte-searched inside `/Applications/RxNPV.app`'s `app.asar` — every new engine present, CSP hash independently recomputed and matching, both new origins in `connect-src`.

---

## Phase 27 — Electron 33 → 44, and how much of it turned out to be automatable

✅ Done. The last item on the findings list, deferred by the user on 2026-09-21 because it was believed to need ten minutes of their time. It needed about three, because most of the "only a human can check this" list turned out to be testable after all.

**What moved:** Electron **33.4.11 → 44.4.4** — eleven majors, Chromium 130 → **152**, Node 20 → 22 — and electron-builder **25.1.8 → 26.15.3**, because 25 predates Electron 44 and would not have packaged it. `npm audit` went from **14 findings to zero**; all fourteen were in packaging-time tooling, exactly as the original assessment said, and the upgrade simply removed them.

**Backup first.** Local Storage was copied to `~/Documents/rxnpv-backups/localstorage-pre-electron-upgrade-*` and the copy confirmed to actually contain the case data — not merely created — before the runtime was touched. Swapping the Chromium underneath a leveldb store is not a thing to do on faith.

**The verification that was supposed to need a human.** A throwaway Electron script exercising the real main-process APIs with no save dialogs, under 44:

| Checked | Result |
|---|---|
| `loadFile` | ok |
| `webContents.printToPDF` | 27,577 bytes, valid `%PDF-` header |
| `webContents.capturePage` | non-empty, returns the requested rect at the display's real 2× device pixel ratio |
| `nativeImage.resize` / `toDataURL` | ok |
| `minWidth` clamping | a request for 500×400 came back as **900×600** — still enforced |

And through the running app itself: the **CSP hash still validates under Chromium 152** (the app renders at all, which it would not if the hash had broken), all seven `contextBridge` methods are exposed, and all four network paths work — the EDGAR main-process bridge returns HTTP 200 and parses, and CT.gov, CMS and Europe PMC all fetch through the CSP.

**One false alarm, recorded so it is not re-investigated.** The probe's own direct `fetch` to SEC returned **403**, which looks exactly like a regression in the main-process fetch. It was the probe sending its own User-Agent instead of the app's `EDGAR_USER_AGENT`; through the app's real bridge the same URL returns 200. SEC rejects unidentified clients. The lesson is narrower than "check the User-Agent": when probing a bridge, drive the bridge, not a reimplementation of it.

**The one user-visible consequence of the jump:** Electron 44 raised the bundle's `LSMinimumSystemVersion` from **11.0 to 13.0**. This machine runs macOS 26.6.2, so it changes nothing here — but the app will no longer launch for anyone on macOS 12 or earlier, which matters only if it is ever shared with someone on an older Mac. Recorded in CLAUDE.md next to the platform line rather than buried here.

**Installed and confirmed.** Packaged with builder 26, installed over `/Applications/RxNPV.app`, launched, and read back: Electron 44.4.4, Chromium 152, renders, and the real saved data intact — the Stok Therapeutics case with its Elsunersen phase-3 program, one watched trial, one custom comp, zero leftover `pdcf_` keys, **zero console errors**. Full 10-file suite clean at 1,032 checks (it runs in jsdom and does not exercise Electron, which is precisely why the live checks above matter).

**What was left for the user** is the two native *save dialogs* — a panel has to appear and actually write a file, and nothing but a person can confirm that — plus a subjective look at window resize and reopen.

---

## Phase 28 — One layout width, and PDF export for charts

✅ Done, both from the user's own review of the upgraded build.

### The layout

Full screen looked wrong and the user was right about why. Each view had hard-coded its own max width — Workspace and Reference Sheet 980, Tools and Simulation 900, Portfolio 1100 — and on this machine's full-screen viewport of **1470 logical points** (a 2560×1664 Retina panel at 2× scaling) that left Tools and Simulation using **61% of the window**, with roughly 285 points of dead space a side. The inconsistency mattered as much as the narrowness: five views disagreeing is what makes a layout read as broken rather than merely conservative.

All five now share `--app-max-width: min(1240px, calc(100vw - 72px))`. Tools and Simulation go from 61% to **82%** of a full-screen window, and every view agrees.

**The half that is easy to miss:** widening the container without capping the prose would have traded one problem for a worse one. Measured after the container change, body copy was running at **188 characters per line** (comfortable is 65–75). So long-form text is capped separately at `--prose-width`, set to **660px — the Reference Sheet's own long-standing `PROSE` constant**, reused rather than invented so the app has one reading measure instead of two. `.note-body` and `.subtle` already carried most of the Simulation side's copy; a `.prose` class covers the React views, applied in the helpers that actually emit body text.

Verified by A/B: Reference Sheet, Simulation, Workspace and Portfolio produce **identical** line-length censuses at 980 and at 1240, confirming their text was already capped and nothing regressed. Only Tools still stretched, and its remaining uncapped blocks — the decoder's fact-row notes, the can/cannot-prove list items, the one-arm analysis explanation and the papers-panel intro — were capped individually. What is deliberately left uncapped: **titles and tables**. A trial title wrapping at 660px would look worse than a long one, and a table should use the width it has.

### PDF export for charts

The user asked why charts could be saved as PNG and SVG but not PDF. The honest answer is that it was never a decision: PNG and SVG are produced entirely in the renderer — serialise the `<svg>`, or rasterise it through a canvas — and **a browser renderer has no PDF writer**. PDF needs Chromium's print engine, which lives in the main process and until now was only wired up for the full report.

`export-chart-pdf` closes that. The SVG arrives already standalone from `svgToStandaloneString()` (styles inlined, explicit dimensions, background rect), so the handler only has to put it on a correctly-sized page and print it — and it stays **vector**, which is the entire point, since a PDF of a rasterised chart would be no better than the PNG.

Two details worth keeping:

- **Fonts are read out of the packaged `rxnpv.html`, not passed through IPC.** The app embeds its typefaces as 11 base64 `@font-face` rules totalling 273KB. An offscreen window would otherwise fall back to a system face and quietly produce a PDF that does not match what is on screen. Reading them in the main process costs nothing per export and cannot drift from what the app ships.
- **The print window is deliberately inert**: no preload, no Node, sandboxed, and `javascript: false`. It renders one SVG this app just produced and nothing else.

Verified end to end short of the save dialog: page requested 9.38in × 2.08in, `MediaBox` came back **675.12 × 150 pt** against 675 × 150 requested; **fonts embedded** (`/FontFile` present); **no `/Subtype /Image`**, so it stayed vector; 10KB for a chart. The button appears alongside PNG / SVG / Panel on every chart, desktop-only and hidden in a browser rather than failing on click.

Full 10-file suite clean at 1,032 checks. (The suite runs in jsdom with no `electronAPI`, so the PDF button correctly does not render there — which is itself the desired behaviour.)

## Phase 29 — Export the whole section, from every section, into a report you choose

✅ Done. From the user: exports were "just the chart itself," which "could get a bit confusing for a reader"; they asked where a pinned item was supposed to go, and wanted "every single chart, graph, and section" exportable to PNG/PDF "either individually or together," fully customisable, containing "ALL of the relevant info from the page."

### What was wrong

Both complaints were accurate. The export wrapper sat around the `<svg>` only, so a revenue chart exported with no title, no "Peak: $1.00B in year 13", and none of the caveats under it. "Panel" was a screenshot of the *screen*, so anything taller than the window, or scrolled out of view, was lost. "Pin to report" said "Pinned" and nothing else, and put a downscaled screenshot at the bottom of a report the user had to know to open.

### What replaced it

**One export bar on every section** — 100+ of them across Workspace, all 18 tools, every Simulation panel, every Reference Sheet card, Portfolio, and each open input card. It offers PNG and PDF of the **whole section** at full height (title, figures, inputs as set, tables, charts, open notes), SVG of any chart in it (with a picker when there are several), and **"+ Report"**. The engine (Phase 28's successor commit `c428f9a`) serialises the section's DOM and renders it offscreen with the app's own styles, so the output is the section itself rather than pixels of the screen. Verified live in the packaged app: the Valuation card, **1,888px tall against an 839px window**, exported complete as a PNG and a 154KB vector PDF, with a footer saying where it came from ("RxNPV · Workspace · Live Check Bio · Valuation · exported …").

**"+ Report" says where it went, and links there.** On a section the report already renders live (revenue chart, cash flow, bridge, SOTP, scenarios) it switches that section on or off rather than storing a copy that would drift. Anywhere else it stores a sanitised HTML snapshot on the chosen case. The report shows these under **Added Sections**, re-themed to the report (a section added in dark mode prints cleanly on a light page — verified live) and scaled to fit the column. The **Sections** panel now lists each one with a checkbox, ↑/↓ and remove, so a PDF can hold exactly the sections chosen, in the order chosen. The Generate Report button carries an "N added" badge.

### Things found along the way

- **A stale case could overwrite newer edits.** Simulation bars are small React roots that are never re-rendered, so a bar holding the case object from when it mounted would have written that old copy back. The bar now re-reads the case at click time, and again after the async snapshot.
- **The test harness had been running against the previous build.** `test_desktop.html` is generated by `setup.js`, and the first full run after the change was green against stale code. Re-running `setup.js` before the suite is now part of the routine, and running once with `--dev` surfaced a pre-existing missing-key warning in the Trial Glossary (hidden by React's production build), now fixed.
- **Collapsed input cards counted as empty sections.** A collapsed `SectionCard` has nothing to export, so it is only declared a section while open. The coverage test caught this.
- **A heading's badge leaked into titles** ("…(Bayesian PoS)→ Forward-looking"). Seen only in the live report screenshot. Titles now skip `.badge`.

### Removed

The chart-only PNG/SVG/Panel rows, `PinToReportButton`, `ExportControls`, the screen-capture pin path, and the `capture-panel` / `capture-panel-data` IPC handlers. There is now one export path, not two.

New `test/export_coverage_test.js`: **403 checks**. Walks every view and fails if any section lacks its own bar, then exercises "+ Report" both ways, the report rendering, include/exclude and reordering. `export_test.js` is up to 28 checks. Full 12-file suite clean.

## Phase 30 — Exports that looked done and were not, and exporting any chart on its own

🔴 → ✅. The user reported that the Fragility Index and 2×2 charts ran off the page, that Sample Size, P-value ↔ CI and Single-Arm CI exported "everything BUT the chart", and that every export was "an image of the inputs". All true, and all shipped after Phase 29 was declared verified — because that verification exported Workspace cards, which do not animate, and never opened a Simulation export.

### What was actually wrong (each found by exporting and looking at the file)

1. **Results and charts printed at zero opacity.** `.results`, `.fade-in` and `.content` fade in over 0.15–0.2s; the offscreen export page was captured the moment it loaded. Section PDFs showed the inputs and a blank space; PNGs showed a faint ghost. Animations and transitions are now off in the export page and in print.
2. **Text that inherits its colour printed near-black on dark.** The app sets `color`/`font-family` on its root div, not `body`; the export page did not, so table cells and subheadings fell back to the browser default.
3. **Chart PDFs printed pale text on white** — the report's print rule (`body: white`) applied inside the export page.
4. **Tall section PNGs repeated their top half** — the 16,000px cap was in CSS pixels, not device pixels (2× on Retina).
5. **38 section/chart PDFs put the footer on a near-empty second page** — pages were sized from the screen layout; now from the print layout, and re-printed larger if they still spill.
6. **Simulation charts ballooned** — viewBox-only SVGs stretched to the 1240px layout (the icon array drew 1158×869). Capped at 1.25× their designed width; icon-array captions wrap instead of being clipped at both ends.
7. **The report cropped wide added sections in print** — an on-screen zoom factor was too large for the narrower printed page. Added sections now reflow to the column.
8. **Titles** built from run-together text or from the export row's own label ("Export section · Export section · …") — fixed at the source; headings added to cards that had none.

### Export any chart on its own

Every chart now has its own **Export chart** row (PNG / PDF / SVG / + Report) exporting that chart alone with its title; every **Export section** row exports everything. Section rows on Simulation panels moved to the top of the panel so a panel's row and a nested card's row never stack.

### How it was verified

New `test/packaged/export_sweep.js` clicks every export button in every view of the packaged app with live data (799 checks over 324 exported images), compares each file with the screen, renders every PDF page with `pdf_pages.swift` (PDFKit), requires single sections to fit one page, builds a report from three single charts and two sections, and writes contact sheets that were opened and read. The final score flags (5) were each opened and matched the screen. Then, in the user's installed app, through the real macOS save dialog: the Fragility Index chart PDF, the Fragility Index section PDF and the Sample Size section PNG — all three opened and correct.

## Phase 31 — The PDF bundle: combine any sections and charts, merged or separate

✅ From the user: a way to take any simulation or tool and put the chart, the inputs and anything else into a single PDF — "they can be separated or merged into one" — the same way "+ Report" works.

**What it is.** Every export row, section or chart, has **+ Bundle**. It stores the same sanitised snapshot "+ Report" does, into a bundle that belongs to no case. **Bundle (N)** in the top bar opens the builder: include/exclude, ↑/↓, remove, clear (two-click), a live preview, and two exports — **one merged PDF** (the bundle page printed like the report, optionally one item per page) and **separate PDFs** (one folder picked once; `01-…`, `02-…` in bundle order). Separate files are rendered by the section renderer through a `batch` mode of `render-section`, not a new IPC channel; names are slugged and writes confined to the chosen folder.

**Found by looking at the merged PDFs, and fixed for the case report too:** a dark frame round every printed page — the page margins are painted with the Electron window's background colour, so `export-pdf` now takes the document's background for the print (an earlier claim in this log that the frame was only an artefact of the image tool was wrong); tall items kept whole jumped a page and left page 1 nearly empty — items may break, charts, table rows and titles may not; table cells split mid-word — `overflow-wrap: anywhere` → `break-word`.

**Verified:** jsdom (collect, view, reorder, include, clear, "+ Bundle" on every row — 654 checks in export_coverage_test), the export sweep's Bundle stage (5 items across Simulation/Tools/Reference; merged PDF and five separate PDFs, every page rendered and read), and on the user's machine through the real dialogs: a Simulation panel and its chart bundled, exported merged (2 pages) and separately (two single-page files), all opened and correct. During the first on-screen session a mis-aimed click had added a chart to the user's case report; it was found from the report's count and removed, leaving the report as it was.


## Phase 32 — UI / UX pass: an automated audit, then eyes on every screen

✅ From the user: "a UI/UX/QOL pass ensuring that the app is attractive and user friendly. run checks on all of the inputs, charts, fonts, buttons, etc."

**The audit.** `test/packaged/ui_audit.js` drives the packaged app through every view, workbench, tool, Simulation tab and sub-tool, Reference tab, Report and Bundle — dark and light at 1470px, dark at 900px — and measures contrast (WCAG AA, opacity compounded), text size, clipping, target size, unnamed buttons, unlabelled inputs, chart names, chart text size, sideways scroll and console errors. First run: 2,125 contrast failures (almost all from export rows faded to 50% opacity), 1,825 small targets, 1,481 pieces of text under 10px, 78 other contrast failures, 48 unlabelled inputs, 37 charts with sub-8px text, 33 charts with no accessible name. Fixed: every 8–9.5px size raised to 10px; export rows made quiet by styling rather than transparency, 26px tall, with a teal hover; placeholders given the theme's tertiary ink (Chromium's default was ~2.3:1 on light); labels on every input the audit named; charts drawn at their measured width instead of a fixed canvas scaled down; every chart given an accessible name. **Final run: zero findings, zero console errors.**

**What the audit could not see, found by looking at its screenshots — several were wrong numbers, not styling:**
- **Monte Carlo percentile bars** were scaled `value / P90`: every negative percentile drew as a 2% stub and a +$0.05 P90 as the full width, so a distribution centred on −$0.15 looked like it sat above zero. Now one shared axis containing zero, bars running from zero, with the axis ends labelled; the P90 tail no longer shares the P10's red.
- **Fragility Index** titled "6 out of 50" over a 100-dot grid with 12 lit. Now one dot per patient up to 200 (50 dots, 6 lit); larger arms keep the per-100 grid with a caption that says so.
- **Portfolio PoS bars** were scaled to the largest PoS, so a lone 9% program drew as a full bar. Now against 100%.
- **Chart axes** gridded at quarter-fractions of the raw range labelled the zero line "$1M" and ticks −17 / 18 / 35 / 53. `niceAxisTicks()` (10 hand-derived checks) now picks round steps with zero always a gridline; the Simulation histogram's min / mid / max x-labels (−0.13, 0.16, 0.45) now use the existing `niceTicks` too (7 checks).
- **Meta-Analysis export row** read "EXPORT SECTION · EXPORT SECTION · …": the row is uppercase by CSS, `innerText` returns it uppercase, and the guard against reading it back was case-sensitive; the heading lookup also only checked the element immediately before the panel. Both fixed, 2 tests.
- **Pipeline risk waterfall** pinned zero at mid-height, leaving the lower half empty and the labels stranded; now scaled to its values, drawn at its real width, with a connector showing the adjustment.
- A caption under the cash-flow chart said negative years were clipped to the axis floor — untrue since the chart gained negative support. Rewritten.
- "−0.00" correlations on the Peak Sales driver chart; truncated names in both peak-sales comp charts; the Discount rate box stretched to 1,100px for two digits and, left blank, used 15% with nothing in the box to say so (now a placeholder says it); Binary Event's example numbers looked like typed values (now "e.g."); the Peak Sales distribution boxes had no labels (now Value / Low / High / Mean / SD / Most likely per distribution, unused boxes disabled); PK/PD's 17 full-width single-field rows regrouped into the grid every other form uses; the price-target grid's shading now explains itself (it is upside vs today's price, so a grid of positive values below the price is all red).

**Verified:** `npm test` and `npm run test:dev` green (13 suites; math_verification 1,130 checks); ui_audit zero findings; export sweep rerun across every view with contact sheets reviewed; screenshots of each fixed screen inspected in both themes.

**Follow-up sweep (same day).** Full suites, packaged checks (offline 26/26, reopen 4/4, live APIs 21/21) and the CT.gov canary all clean; then the screens not reviewed in the first pass. Found and fixed:
- **NNT confidence interval printed high-to-low** ("13.2 to 3.1") whenever the risk difference was negative — `computeNNT` paired `lower` with `1/rd.upper`, which is only the smaller number for a positive RD. The two math checks on it had asserted that same formula rather than a hand-worked number, so they passed; they now check 3.08681 / 13.15080 worked longhand, plus the swapped-arms table.
- **Dates stamped in UTC.** Report, bundle and auto-logged Evidence Log entries used `toISOString()`, so after ~8pm US time they carried tomorrow's date. New `localDateStamp()`.
- The Cash Runway chart caption claimed negative cash was "clamped to zero for display"; it is not, and the negative stretch is meaningful (cumulative cash the plan needs raised). Rewritten.
- Export titles from disclosure headings carried the arrow ("▸ SECONDARY ENDPOINTS").

Math checks 1,130; export_test 31.
